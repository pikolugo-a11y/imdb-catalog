import 'server-only';
import {db} from './db';
const pnum=n=>Math.max(1,Number(n)||1),psz=n=>Math.min(100,Math.max(20,Number(n)||50));
export async function getIdentityValidationStats(){const sql=db();const[r]=await sql`SELECT
 count(*)::int total,
 count(*) FILTER(WHERE v.imdb_original_title IS NOT NULL AND v.imdb_year IS NOT NULL)::int imdb_cached,
 count(*) FILTER(WHERE v.tmdb_id=m.tmdb_id AND v.tmdb_original_title IS NOT NULL AND v.tmdb_year IS NOT NULL)::int tmdb_cached,
 count(*) FILTER(WHERE v.fa_id=m.fa_id AND v.fa_original_title IS NOT NULL AND v.fa_year IS NOT NULL)::int fa_cached,
 count(*) FILTER(WHERE v.imdb_original_title IS NOT NULL AND v.imdb_year IS NOT NULL AND v.tmdb_id=m.tmdb_id AND v.tmdb_original_title IS NOT NULL AND v.tmdb_year IS NOT NULL AND v.fa_id=m.fa_id AND v.fa_original_title IS NOT NULL AND v.fa_year IS NOT NULL)::int ready_three,
 count(*) FILTER(WHERE COALESCE(v.validation_status,'pending_data') IN('pending_data','revalidation_pending'))::int pending
 FROM catalog_lifecycle cl JOIN movies m USING(imdb_id) LEFT JOIN identity_validation v USING(imdb_id)
 WHERE cl.lifecycle_state='IDENTITY_VALIDATION'`;return r||{}}
export async function getIdentityValidationPage(filters={}){const sql=db(),page=pnum(filters.page),pageSize=psz(filters.pageSize),offset=(page-1)*pageSize,type=String(filters.type||''),q=String(filters.q||'').trim().toLowerCase(),year=Number(filters.year)||0;const where=sql`cl.lifecycle_state='IDENTITY_VALIDATION' AND (${type}='' OR (${type}='movie' AND m.type='Película') OR (${type}='series' AND m.type IN('Serie','Miniserie'))) AND (${year}=0 OR m.year=${year}) AND (${q}='' OR lower(COALESCE(m.title_es,m.title,m.original_title,'')) LIKE ${'%'+q+'%'} OR lower(m.imdb_id) LIKE ${'%'+q+'%'} OR lower(COALESCE(m.tmdb_id,'')) LIKE ${'%'+q+'%'} OR lower(COALESCE(m.fa_id,'')) LIKE ${'%'+q+'%'})`;
 const [c,rows]=await Promise.all([sql`SELECT count(*)::int total FROM catalog_lifecycle cl JOIN movies m USING(imdb_id) LEFT JOIN identity_validation v USING(imdb_id) WHERE ${where}`,sql`SELECT m.imdb_id,m.type,COALESCE(m.title_es,m.title,m.original_title) display_title,m.original_title,m.year,m.poster_path,m.tmdb_id,m.fa_id,COALESCE(v.validation_status,'pending_data') automatic_status,v.validation_score,v.validation_details,v.suspected_source,v.imdb_title,v.imdb_original_title,v.imdb_year,v.tmdb_title_es,v.tmdb_original_title,v.tmdb_year,v.fa_title_es,v.fa_original_title,v.fa_year,v.validated_at,v.full_refresh_state,v.full_refresh_error,v.full_refreshed_at,(v.validation_details->'manual') manual_review,COALESCE(v.validation_status,'pending_data') validation_status FROM catalog_lifecycle cl JOIN movies m USING(imdb_id) LEFT JOIN identity_validation v USING(imdb_id) WHERE ${where} ORDER BY m.year DESC NULLS LAST,m.imdb_id LIMIT ${pageSize} OFFSET ${offset}`]);const total=Number(c[0]?.total||0);return{rows,total,page,pageSize,pages:Math.max(1,Math.ceil(total/pageSize))}}
export async function getIdentityValidationRun(){const sql=db();const[r]=await sql`SELECT id,status,started_at,finished_at,processed_count,updated_count,error_count,summary,updated_at FROM pipeline_runs WHERE job_type='identity_validation' ORDER BY created_at DESC LIMIT 1`;return r||null}
