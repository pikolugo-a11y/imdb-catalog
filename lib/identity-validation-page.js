import 'server-only';
import {db} from './db';
const pnum=n=>Math.max(1,Number(n)||1),psz=n=>Math.min(100,Math.max(20,Number(n)||50));
const readySql=`v.tmdb_id=m.tmdb_id AND ((v.imdb_original_title IS NOT NULL AND v.imdb_year IS NOT NULL AND v.tmdb_original_title IS NOT NULL AND v.tmdb_year IS NOT NULL) OR COALESCE((v.validation_details #>> '{link_evidence,forward_match}')::boolean,false) OR COALESCE((v.validation_details #>> '{link_evidence,reverse_match}')::boolean,false))`;
export async function getIdentityValidationStats(){const sql=db();const rows=await sql.unsafe(`SELECT count(*)::int total,count(*) FILTER(WHERE cl.lifecycle_state='IDENTITY_VALIDATION' AND NOT(${readySql}))::int evidence_pending,count(*) FILTER(WHERE cl.lifecycle_state='IDENTITY_VALIDATION' AND ${readySql})::int ready,count(*) FILTER(WHERE cl.lifecycle_state='IDENTITY_REVIEW_REQUIRED' AND COALESCE(v.validation_score,0)>=60)::int doubtful,count(*) FILTER(WHERE cl.lifecycle_state='IDENTITY_REVIEW_REQUIRED' AND COALESCE(v.validation_score,0)<60)::int probable_error FROM catalog_lifecycle cl JOIN movies m USING(imdb_id) LEFT JOIN identity_validation v USING(imdb_id) WHERE cl.lifecycle_state IN('IDENTITY_VALIDATION','IDENTITY_REVIEW_REQUIRED')`);return rows[0]||{}}
export async function getIdentityValidationPage(filters={}){const sql=db(),page=pnum(filters.page),pageSize=psz(filters.pageSize),offset=(page-1)*pageSize,substate=String(filters.substate||''),type=String(filters.type||''),q=String(filters.q||'').trim().toLowerCase(),year=Number(filters.year)||0;const ready=sql`v.tmdb_id=m.tmdb_id AND ((v.imdb_original_title IS NOT NULL AND v.imdb_year IS NOT NULL AND v.tmdb_original_title IS NOT NULL AND v.tmdb_year IS NOT NULL) OR COALESCE((v.validation_details #>> '{link_evidence,forward_match}')::boolean,false) OR COALESCE((v.validation_details #>> '{link_evidence,reverse_match}')::boolean,false))`;const where=sql`cl.lifecycle_state IN('IDENTITY_VALIDATION','IDENTITY_REVIEW_REQUIRED') AND (${type}='' OR (${type}='movie' AND m.type='Película') OR (${type}='series' AND m.type IN('Serie','Miniserie'))) AND (${year}=0 OR m.year=${year}) AND (${q}='' OR lower(COALESCE(m.title_es,m.title,m.original_title,'')) LIKE ${'%'+q+'%'} OR lower(m.imdb_id) LIKE ${'%'+q+'%'} OR lower(COALESCE(m.tmdb_id,'')) LIKE ${'%'+q+'%'}) AND (${substate}='' OR (${substate}='evidence_pending' AND cl.lifecycle_state='IDENTITY_VALIDATION' AND NOT(${ready})) OR (${substate}='ready' AND cl.lifecycle_state='IDENTITY_VALIDATION' AND ${ready}) OR (${substate}='doubtful' AND cl.lifecycle_state='IDENTITY_REVIEW_REQUIRED' AND COALESCE(v.validation_score,0)>=60) OR (${substate}='probable_error' AND cl.lifecycle_state='IDENTITY_REVIEW_REQUIRED' AND COALESCE(v.validation_score,0)<60))`;const[c,rows]=await Promise.all([sql`SELECT count(*)::int total FROM catalog_lifecycle cl JOIN movies m USING(imdb_id) LEFT JOIN identity_validation v USING(imdb_id) WHERE ${where}`,sql`SELECT m.imdb_id,m.type,COALESCE(m.title_es,m.title,m.original_title) display_title,m.original_title,m.year,m.poster_path,m.tmdb_id,v.validation_status,v.validation_score,v.validation_details,v.suspected_source,v.imdb_title,v.imdb_original_title,v.imdb_year,v.tmdb_title_es,v.tmdb_original_title,v.tmdb_year,v.validated_at,(v.validation_details->'manual') manual_review,CASE WHEN cl.lifecycle_state='IDENTITY_REVIEW_REQUIRED' AND COALESCE(v.validation_score,0)>=60 THEN 'doubtful' WHEN cl.lifecycle_state='IDENTITY_REVIEW_REQUIRED' THEN 'probable_error' WHEN ${ready} THEN 'ready' ELSE 'evidence_pending' END substate FROM catalog_lifecycle cl JOIN movies m USING(imdb_id) LEFT JOIN identity_validation v USING(imdb_id) WHERE ${where} ORDER BY CASE WHEN cl.lifecycle_state='IDENTITY_REVIEW_REQUIRED' THEN 0 ELSE 1 END,v.validation_score NULLS FIRST,m.year DESC NULLS LAST,m.imdb_id LIMIT ${pageSize} OFFSET ${offset}`]);const total=Number(c[0]?.total||0);return{rows,total,page,pageSize,pages:Math.max(1,Math.ceil(total/pageSize))}}
