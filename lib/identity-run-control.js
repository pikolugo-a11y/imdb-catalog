import 'server-only';
import {db} from './db';
import {audit,errorInfo,startRun,finishRun} from './runlog';

const DISPATCH_URL='https://api.github.com/repos/pikolugo-a11y/imdb-catalog/actions/workflows/identity-full-refresh.yml/dispatches';
export async function getIdentityRunState(){const sql=db();const [r]=await sql`SELECT id,status,started_at,finished_at,processed_count,updated_count,error_count,summary,updated_at FROM pipeline_runs WHERE job_type='identity_scan' ORDER BY created_at DESC LIMIT 1`;return r||null}
export async function dispatchIdentityRefresh(source='web'){
 const sql=db();const [running]=await sql`SELECT id,processed_count,summary FROM pipeline_runs WHERE job_type='identity_scan' AND status='running' AND updated_at>now()-interval '3 hours' ORDER BY created_at DESC LIMIT 1`;
 if(running)return{ok:true,alreadyRunning:true,runId:running.id,processed:Number(running.processed_count||0),total:Number(running.summary?.total_unique||0)};
 const [c]=await sql`SELECT count(*)::int total FROM movies m LEFT JOIN catalog_exclusions ex USING(imdb_id) WHERE ex.imdb_id IS NULL AND m.imdb_id~'^tt[0-9]+$' AND (m.tmdb_id IS NULL OR m.fa_id IS NULL)`;
 const total=Number(c?.total||0),run=await startRun('identity_scan',source,{stage:'queued',scope:'missing_ids_only',total_unique:total,progress_pct:0,current_block:'Preparando'}),token=process.env.GITHUB_ACTIONS_TOKEN;
 if(!total){await finishRun(run.id,'success',{processed:0,updated:0,summary:{stage:'done',scope:'missing_ids_only',total_unique:0,progress_pct:100}});return{ok:true,runId:run.id,total:0,alreadyRunning:false,message:'No hay IDs pendientes'}}
 if(!token){await finishRun(run.id,'failed',{errors:1,summary:{stage:'dispatch_failed',error:{message:'Falta GITHUB_ACTIONS_TOKEN'}}});return{ok:false,message:'Falta GITHUB_ACTIONS_TOKEN'}}
 try{const r=await fetch(DISPATCH_URL,{method:'POST',headers:{Authorization:`Bearer ${token}`,Accept:'application/vnd.github+json','X-GitHub-Api-Version':'2022-11-28','User-Agent':'PikoFilm/3.0'},body:JSON.stringify({ref:'main',inputs:{run_id:String(run.id)}}),cache:'no-store'});if(!r.ok)throw new Error(`GitHub workflow dispatch HTTP ${r.status}: ${(await r.text()).slice(0,240)}`);await audit('identity','pipeline_run',String(run.id),'refresh_dispatched',{total_unique:total,scope:'missing_ids_only'});return{ok:true,runId:run.id,total,alreadyRunning:false}}catch(e){await finishRun(run.id,'failed',{errors:1,summary:{stage:'dispatch_failed',error:errorInfo(e)}});await audit('identity','pipeline_run',String(run.id),'refresh_dispatch_failed',{error:errorInfo(e)});return{ok:false,message:e?.message||'No se pudo lanzar la reevaluación'}}
}
export async function cancelIdentityRefresh(runId){const sql=db(),id=Number(runId);if(!id)return{ok:false,message:'Ejecución inválida'};const [r]=await sql`UPDATE pipeline_runs SET summary=COALESCE(summary,'{}'::jsonb)||jsonb_build_object('cancel_requested',true,'stage','cancel_requested','cancel_requested_at',now()),updated_at=now() WHERE id=${id} AND job_type='identity_scan' AND status='running' RETURNING id`;if(!r)return{ok:false,message:'La ejecución ya no está activa'};await audit('identity','pipeline_run',String(id),'cancel_requested',{});return{ok:true,message:'Parada solicitada. Se detendrá al terminar el bloque actual.'}}
