import 'server-only';
import {db} from './db';
import {getDataQualityTitle} from './data-quality';
import {ensureImdbRating} from './imdb-rating-on-demand';
import {recomputeLifecycleForIds} from './lifecycle';
import {audit} from './runlog';

const clean=v=>v&&v!=='N/A'?String(v).trim():null;
const list=v=>String(v||'').split(',').map(x=>x.trim()).filter(Boolean);
const omdbKey=()=>process.env.OMDB_API_KEY||process.env.OMDB_APIKEY||'';
const faHeaders={'User-Agent':'Mozilla/5.0 (compatible; PikoFilm/1.0; personal non-commercial)','Accept-Language':'es-ES,es;q=0.9'};
const parseRuntime=v=>{const s=String(v||'');const m=s.match(/(\d+)\s*min/i);return m?Number(m[1]):null};

async function sourceStep(imdbId,source,fn){const started=Date.now();await audit('data_quality','title',imdbId,'source_refresh_started',{source});try{const changed=await fn();await audit('data_quality','title',imdbId,'source_refresh_completed',{source,changed,duration_ms:Date.now()-started});return{source,ok:true,changed};}catch(e){const message=e?.message||String(e);await audit('data_quality','title',imdbId,'source_refresh_failed',{source,message,duration_ms:Date.now()-started});return{source,ok:false,changed:[],error:message};}}

async function setExternalPoster(sql,imdbId,url,source){if(!url)return;const patch={data_quality_external_poster:{url,source,checked_at:new Date().toISOString()}};await sql`UPDATE movies SET source_status=COALESCE(source_status,'{}'::jsonb)||${JSON.stringify(patch)}::jsonb,synced_at=now() WHERE imdb_id=${imdbId}`;}
async function setExternalPeople(sql,imdbId,{director,cast},source){const patch={};if(director)patch.data_quality_external_director={value:director,source};if(cast?.length)patch.data_quality_external_cast={value:cast,source};if(Object.keys(patch).length)await sql`UPDATE movies SET source_status=COALESCE(source_status,'{}'::jsonb)||${JSON.stringify(patch)}::jsonb,synced_at=now() WHERE imdb_id=${imdbId}`;}

async function refreshTmdb(imdbId){const sql=db(),before=await getDataQualityTitle(imdbId),[m]=await sql`SELECT tmdb_id,type FROM movies WHERE imdb_id=${imdbId}`;if(!m?.tmdb_id)throw new Error('Falta TMDb ID');const token=process.env.TMDB_API_TOKEN;if(!token)throw new Error('Falta TMDB_API_TOKEN');const media=m.type==='Serie'||m.type==='Miniserie'?'tv':'movie';const r=await fetch(`https://api.themoviedb.org/3/${media}/${m.tmdb_id}?language=es-ES&append_to_response=credits`,{headers:{Authorization:`Bearer ${token}`,Accept:'application/json'},cache:'no-store'});if(!r.ok)throw new Error(`TMDb HTTP ${r.status}`);const d=await r.json();const title=d.title||d.name||null,original=d.original_title||d.original_name||null,release=d.release_date||d.first_air_date||null,year=Number(String(release||'').slice(0,4))||null,runtime=d.runtime||d.episode_run_time?.[0]||null,country=(d.production_countries||d.origin_country||[]).map(x=>x.name||x).filter(Boolean).join(', ')||null,genres=(d.genres||[]).map(x=>x.name).filter(Boolean),director=(d.credits?.crew||[]).find(x=>x.job==='Director'),cast=(d.credits?.cast||[]).slice(0,15),changed=[];
 await sql`UPDATE movies SET type=COALESCE(type,${media==='tv'?'Serie':'Película'}),title_es=COALESCE(title_es,${title}),original_title=COALESCE(original_title,${original}),year=COALESCE(year,${year}),runtime=COALESCE(runtime,${runtime}),country=COALESCE(country,${country}),tmdb_rating=${d.vote_average??null},tmdb_votes=${d.vote_count??null},poster_path=COALESCE(poster_path,${d.poster_path||null}),backdrop_path=COALESCE(backdrop_path,${d.backdrop_path||null}),synced_at=now() WHERE imdb_id=${imdbId}`;
 for(const[k,v]of Object.entries({type:media==='tv'?'Serie':'Película',title_es:title,original_title:original,year,runtime,country,tmdb_rating:d.vote_average,tmdb_votes:d.vote_count,poster_path:d.poster_path,backdrop_path:d.backdrop_path}))if(v!=null&&v!==''&&(!before?.[k]||k==='tmdb_rating'||k==='tmdb_votes'))changed.push(k);
 const overview=!before?.overview?d.overview||null:null,language=!before?.original_language?d.original_language||null:null,rel=!before?.release_date?release:null;if(overview||language||rel){await sql`INSERT INTO movie_metadata(imdb_id,overview,original_language,release_date,metadata_enriched_at,metadata_source) VALUES(${imdbId},${overview},${language},${rel},now(),'tmdb') ON CONFLICT(imdb_id) DO UPDATE SET overview=COALESCE(movie_metadata.overview,EXCLUDED.overview),original_language=COALESCE(movie_metadata.original_language,EXCLUDED.original_language),release_date=COALESCE(movie_metadata.release_date,EXCLUDED.release_date),metadata_enriched_at=now(),metadata_source='tmdb'`;if(overview)changed.push('overview');if(language)changed.push('original_language');if(rel)changed.push('release_date');}
 if(!before?.genres&&genres.length){for(const g of genres)await sql`INSERT INTO movie_genres(imdb_id,genre) VALUES(${imdbId},${g}) ON CONFLICT DO NOTHING`;changed.push('genres');}
 if(!before?.director&&director){await sql`INSERT INTO people(tmdb_person_id,name,profile_path,known_for_department,updated_at) VALUES(${String(director.id)},${director.name},${director.profile_path||null},${director.known_for_department||'Directing'},now()) ON CONFLICT(tmdb_person_id) DO UPDATE SET name=EXCLUDED.name,profile_path=EXCLUDED.profile_path,updated_at=now()`;await sql`INSERT INTO movie_credits(imdb_id,tmdb_person_id,credit_type,character_name,job,credit_order) VALUES(${imdbId},${String(director.id)},'crew','','Director',NULL) ON CONFLICT DO NOTHING`;changed.push('director');}
 if(!before?.cast&&cast.length){for(const a of cast){await sql`INSERT INTO people(tmdb_person_id,name,profile_path,known_for_department,updated_at) VALUES(${String(a.id)},${a.name},${a.profile_path||null},${a.known_for_department||'Acting'},now()) ON CONFLICT(tmdb_person_id) DO UPDATE SET name=EXCLUDED.name,profile_path=EXCLUDED.profile_path,updated_at=now()`;await sql`INSERT INTO movie_credits(imdb_id,tmdb_person_id,credit_type,character_name,job,credit_order) VALUES(${imdbId},${String(a.id)},'cast',${a.character||''},'',${a.order??0}) ON CONFLICT DO NOTHING`;}changed.push('cast');}
 return changed;}

async function refreshOmdb(imdbId){const sql=db(),before=await getDataQualityTitle(imdbId),key=omdbKey();if(!key)throw new Error('Falta OMDB_API_KEY');const r=await fetch(`https://www.omdbapi.com/?apikey=${encodeURIComponent(key)}&i=${encodeURIComponent(imdbId)}&plot=full`,{cache:'no-store'});if(!r.ok)throw new Error(`OMDb HTTP ${r.status}`);const j=await r.json();if(j.Response==='False')throw new Error(j.Error||'OMDb no encontró el título');const runtime=!before?.runtime?parseRuntime(j.Runtime):null,country=!before?.country?clean(j.Country):null,originalTitle=!before?.original_title?clean(j.Title):null,year=!before?.year?Number(String(j.Year||'').slice(0,4))||null:null,poster=!before?.poster_path&&!before?.external_poster_url?clean(j.Poster):null,overview=!before?.overview?clean(j.Plot):null,language=!before?.original_language?clean(j.Language):null,release=!before?.release_date&&j.Released&&j.Released!=='N/A'?new Date(j.Released):null,imdbRating=j.imdbRating&&j.imdbRating!=='N/A'?Number(j.imdbRating):null,imdbVotes=j.imdbVotes&&j.imdbVotes!=='N/A'?Number(String(j.imdbVotes).replace(/[^0-9]/g,'')):null,changed=[];
 const rt=Number(String((j.Ratings||[]).find(x=>x.Source==='Rotten Tomatoes')?.Value||'').replace('%',''))||null,meta=Number(j.Metascore)||null;const scorePatch={data_quality_external_scores:{rotten_tomatoes:rt,metacritic:meta,checked_at:new Date().toISOString()}};
 await sql`UPDATE movies SET runtime=COALESCE(runtime,${runtime}),country=COALESCE(country,${country}),original_title=COALESCE(original_title,${originalTitle}),year=COALESCE(year,${year}),imdb_rating=${imdbRating},imdb_votes=${imdbVotes},source_status=COALESCE(source_status,'{}'::jsonb)||${JSON.stringify(scorePatch)}::jsonb,synced_at=now() WHERE imdb_id=${imdbId}`;
 for(const[k,v]of Object.entries({runtime,country,original_title:originalTitle,year,imdb_rating:imdbRating,imdb_votes:imdbVotes}))if(v!=null&&(!before?.[k]||k==='imdb_rating'||k==='imdb_votes'))changed.push(k);
 if(overview||language||release){await sql`INSERT INTO movie_metadata(imdb_id,overview,original_language,release_date,metadata_enriched_at,metadata_source) VALUES(${imdbId},${overview},${language},${release},now(),'omdb') ON CONFLICT(imdb_id) DO UPDATE SET overview=COALESCE(movie_metadata.overview,EXCLUDED.overview),original_language=COALESCE(movie_metadata.original_language,EXCLUDED.original_language),release_date=COALESCE(movie_metadata.release_date,EXCLUDED.release_date),metadata_enriched_at=now(),metadata_source=CASE WHEN movie_metadata.overview IS NULL OR movie_metadata.original_language IS NULL OR movie_metadata.release_date IS NULL THEN 'omdb' ELSE movie_metadata.metadata_source END`;if(overview)changed.push('overview');if(language)changed.push('original_language');if(release)changed.push('release_date');}
 if(!before?.genres){const genres=list(j.Genre);for(const g of genres)await sql`INSERT INTO movie_genres(imdb_id,genre) VALUES(${imdbId},${g}) ON CONFLICT DO NOTHING`;if(genres.length)changed.push('genres');}
 if(poster){await setExternalPoster(sql,imdbId,poster,'OMDb');changed.push('poster_path');}
 const director=!before?.director?clean(j.Director):null,cast=!before?.cast?list(j.Actors):[];if(director||cast.length){await setExternalPeople(sql,imdbId,{director,cast},'OMDb');if(director)changed.push('director');if(cast.length)changed.push('cast');}
 if(rt!=null)changed.push('rotten_tomatoes');if(meta!=null)changed.push('metacritic');return changed;}

function parseFa(html){let out={};for(const m of html.matchAll(/<script[^>]+type=["']application\/ld\+json["'][^>]*>([\s\S]*?)<\/script>/gi)){try{const d=JSON.parse(m[1].trim()),arr=Array.isArray(d)?d:[d];for(const n of arr){const a=n?.aggregateRating;if(a?.ratingValue!=null)out.rating=Number(String(a.ratingValue).replace(',','.'));if(a?.ratingCount!=null)out.votes=Number(String(a.ratingCount).replace(/[^0-9]/g,''));out.title=out.title||n?.name||null;}}catch{}}return out;}
async function refreshFa(imdbId){const sql=db(),before=await getDataQualityTitle(imdbId),[m]=await sql`SELECT fa_id FROM movies WHERE imdb_id=${imdbId}`;if(!m?.fa_id)throw new Error('Falta FilmAffinity ID');const r=await fetch(`https://www.filmaffinity.com/es/film${m.fa_id}.html`,{headers:faHeaders,cache:'no-store'});if(!r.ok)throw new Error(`FilmAffinity HTTP ${r.status}`);const p=parseFa(await r.text());if(!Number.isFinite(p.rating)&&!Number.isFinite(p.votes))throw new Error('FilmAffinity no devolvió nota ni votos');await sql`UPDATE movies SET title_es=COALESCE(title_es,${p.title||null}),fa_rating=${Number.isFinite(p.rating)?p.rating:null},fa_votes=${Number.isFinite(p.votes)?p.votes:null},synced_at=now() WHERE imdb_id=${imdbId}`;const changed=[];if(!before?.title_es&&p.title)changed.push('title_es');if(Number.isFinite(p.rating))changed.push('fa_rating');if(Number.isFinite(p.votes))changed.push('fa_votes');return changed;}

async function refreshImdbFallback(imdbId){const before=await getDataQualityTitle(imdbId);if(before?.imdb_rating&&before?.imdb_votes)return[];const r=await ensureImdbRating(imdbId,{timeoutMs:20000});const changed=[];if(r){const after=await getDataQualityTitle(imdbId);if(!before?.imdb_rating&&after?.imdb_rating)changed.push('imdb_rating');if(!before?.imdb_votes&&after?.imdb_votes)changed.push('imdb_votes');}return changed;}

export async function updateDataQualityTitle(imdbId){if(!/^tt\d+$/.test(String(imdbId||'')))throw new Error('IMDb ID inválido');const before=await getDataQualityTitle(imdbId);if(!before)throw new Error('Título no encontrado');if(before.validation_status!=='valid')throw new Error('La identidad debe estar validada antes de actualizar datos');await audit('data_quality','title',imdbId,'unitary_refresh_started',{coverage:before.coverage,missing:before.missingCritical});const results=[];results.push(await sourceStep(imdbId,'TMDb',()=>refreshTmdb(imdbId)));results.push(await sourceStep(imdbId,'OMDb',()=>refreshOmdb(imdbId)));results.push(await sourceStep(imdbId,'IMDb fallback',()=>refreshImdbFallback(imdbId)));results.push(await sourceStep(imdbId,'FilmAffinity',()=>refreshFa(imdbId)));
 const sql=db(),ratingsTouched=results.some(r=>r.changed.some(x=>['imdb_rating','imdb_votes','fa_rating','fa_votes','tmdb_rating','tmdb_votes'].includes(x)));if(ratingsTouched)await sql`UPDATE movies SET final_rating=NULL,synced_at=now() WHERE imdb_id=${imdbId}`;const after=await getDataQualityTitle(imdbId);const lifecycle=(await recomputeLifecycleForIds([imdbId])).get(imdbId);await audit('data_quality','title',imdbId,'unitary_refresh_completed',{coverage_before:before.coverage,coverage_after:after?.coverage,missing:after?.missingCritical,ratings_invalidated:ratingsTouched,lifecycle:lifecycle?.state,results});return{before,after,results,lifecycle,ratingsInvalidated:ratingsTouched};}
