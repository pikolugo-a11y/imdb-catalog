import 'server-only';
import {db} from './db';

const clampPage=n=>Math.max(1,Number(n)||1),clampSize=n=>Math.min(100,Math.max(20,Number(n)||50));
export async function getIdentityCatalogPage(filters={}){const sql=db(),page=clampPage(filters.page),pageSize=clampSize(filters.pageSize),offset=(page-1)*pageSize,type=String(filters.type||''),q=String(filters.q||'').trim().toLowerCase(),issue=String(filters.issue||'');const where=sql`FROM movies m LEFT JOIN catalog_exclusions ex USING(imdb_id) WHERE ex.imdb_id IS NULL AND (m.imdb_id IS NULL OR m.imdb_id!~'^tt[0-9]+$' OR m.tmdb_id IS NULL OR m.fa_id IS NULL OR COALESCE((m.source_status->>'tmdb'),'')='mismatch' OR COALESCE((m.source_status->>'filmaffinity'),'')='mismatch') AND (${type}='' OR (${type}='movie' AND m.type='Película') OR (${type}='series' AND m.type IN ('Serie','Miniserie'))) AND (${q}='' OR lower(COALESCE(m.title_es,m.title,m.original_title,'')) LIKE ${'%'+q+'%'}) AND (${issue}='' OR (${issue}='missing_tmdb' AND m.tmdb_id IS NULL) OR (${issue}='missing_fa' AND m.fa_id IS NULL) OR (${issue}='doubtful' AND (COALESCE((m.source_status->>'tmdb'),'')='mismatch' OR COALESCE((m.source_status->>'filmaffinity'),'')='mismatch')))`;const [countRows,rows]=await Promise.all([sql`SELECT count(*)::int total ${where}`,sql`SELECT m.imdb_id,m.type,COALESCE(m.title_es,m.title,m.original_title) display_title,m.original_title,m.year,m.tmdb_id,m.fa_id,m.source_status,(m.imdb_id IS NULL OR m.imdb_id!~'^tt[0-9]+$') missing_imdb,(m.tmdb_id IS NULL) missing_tmdb,(m.fa_id IS NULL) missing_fa,(COALESCE((m.source_status->>'tmdb'),'')='mismatch' OR COALESCE((m.source_status->>'filmaffinity'),'')='mismatch') doubtful ${where} ORDER BY CASE WHEN COALESCE((m.source_status->>'tmdb'),'')='mismatch' OR COALESCE((m.source_status->>'filmaffinity'),'')='mismatch' THEN 0 WHEN m.tmdb_id IS NULL AND m.fa_id IS NULL THEN 1 WHEN m.tmdb_id IS NULL THEN 2 ELSE 3 END,m.year DESC NULLS LAST,m.imdb_id LIMIT ${pageSize} OFFSET ${offset}`]);const total=Number(countRows[0]?.total||0);return{rows,total,page,pageSize,pages:Math.max(1,Math.ceil(total/pageSize))}}

export async function getPlexIdentityPage(filters={}){const sql=db(),page=clampPage(filters.page),pageSize=clampSize(filters.pageSize),offset=(page-1)*pageSize,q=String(filters.q||'').trim().toLowerCase();const [countRows,rows]=await Promise.all([sql`SELECT count(*)::int total FROM plex_items p WHERE p.active AND p.item_type IN('movie','show') AND NOT EXISTS(SELECT 1 FROM plex_external_ids i WHERE i.rating_key=p.rating_key AND i.provider='imdb') AND (${q}='' OR lower(COALESCE(p.plex_title,'')) LIKE ${'%'+q+'%'})`,sql`SELECT p.rating_key,p.plex_title display_title,p.plex_year year,p.item_type,max(x.external_id) FILTER(WHERE x.provider='tmdb') tmdb_id,max(x.external_id) FILTER(WHERE x.provider IN('filmaffinity','fa')) fa_id,array_agg(x.provider||':'||x.external_id) FILTER(WHERE x.provider IS NOT NULL) known_ids FROM plex_items p LEFT JOIN plex_external_ids x ON x.rating_key=p.rating_key WHERE p.active AND p.item_type IN('movie','show') AND NOT EXISTS(SELECT 1 FROM plex_external_ids i WHERE i.rating_key=p.rating_key AND i.provider='imdb') AND (${q}='' OR lower(COALESCE(p.plex_title,'')) LIKE ${'%'+q+'%'}) GROUP BY p.rating_key,p.plex_title,p.plex_year,p.item_type,p.added_at ORDER BY p.added_at DESC NULLS LAST LIMIT ${pageSize} OFFSET ${offset}`]);const total=Number(countRows[0]?.total||0);return{rows,total,page,pageSize,pages:Math.max(1,Math.ceil(total/pageSize))}}
