import {PIKOQUALITY_C6_VERSION,scorePikoQualityC6,c6Band} from './pikoquality-c6-core.mjs';

async function rebuildAffectedAggregates(sql,keys){
  const groups=await sql`SELECT DISTINCT grandparent_rating_key,parent_index FROM plex_items WHERE rating_key=ANY(${keys}) AND item_type='episode' AND grandparent_rating_key IS NOT NULL AND parent_index IS NOT NULL`;
  const shows=new Set();let seasons=0;
  for(const g of groups){
    const show=String(g.grandparent_rating_key),season=Number(g.parent_index);shows.add(show);
    const[row]=await sql`WITH scored AS (SELECT q.score FROM plex_items p JOIN plex_technical_state pts ON pts.rating_key=p.rating_key AND pts.snapshot_status='ready' JOIN piko_quality q ON q.rating_key=p.rating_key WHERE p.active AND p.item_type='episode' AND p.grandparent_rating_key=${show} AND p.parent_index=${season} AND q.status='evaluated' AND q.formula_version=${PIKOQUALITY_C6_VERSION} AND q.source_fingerprint IS NOT DISTINCT FROM pts.technical_fingerprint), totals AS (SELECT count(*)::int total_count FROM plex_items WHERE active AND item_type='episode' AND grandparent_rating_key=${show} AND parent_index=${season}) SELECT count(*)::int analyzed_count,(SELECT total_count FROM totals) total_count,round((0.75*percentile_cont(0.5) WITHIN GROUP(ORDER BY score)+0.25*percentile_cont(0.1) WITHIN GROUP(ORDER BY score))::numeric)::int score FROM scored`;
    if(!row||!Number(row.analyzed_count||0))continue;
    const score=Number(row.score),band=c6Band(score),entityKey=`${show}:${season}`;
    await sql`INSERT INTO piko_quality_aggregates(entity_type,entity_key,parent_key,season_index,score,band,analyzed_count,total_count,formula_version,updated_at) VALUES('season',${entityKey},${show},${season},${score},${band},${Number(row.analyzed_count)},${Number(row.total_count||0)},${PIKOQUALITY_C6_VERSION},now()) ON CONFLICT(entity_type,entity_key) DO UPDATE SET parent_key=EXCLUDED.parent_key,season_index=EXCLUDED.season_index,score=EXCLUDED.score,band=EXCLUDED.band,analyzed_count=EXCLUDED.analyzed_count,total_count=EXCLUDED.total_count,formula_version=EXCLUDED.formula_version,updated_at=now()`;
    seasons++;
  }
  let rebuiltShows=0;
  for(const show of shows){
    const[row]=await sql`WITH scored AS (SELECT q.score FROM plex_items p JOIN plex_technical_state pts ON pts.rating_key=p.rating_key AND pts.snapshot_status='ready' JOIN piko_quality q ON q.rating_key=p.rating_key WHERE p.active AND p.item_type='episode' AND p.grandparent_rating_key=${show} AND q.status='evaluated' AND q.formula_version=${PIKOQUALITY_C6_VERSION} AND q.source_fingerprint IS NOT DISTINCT FROM pts.technical_fingerprint), totals AS (SELECT count(*)::int total_count FROM plex_items WHERE active AND item_type='episode' AND grandparent_rating_key=${show}) SELECT count(*)::int analyzed_count,(SELECT total_count FROM totals) total_count,round((0.75*percentile_cont(0.5) WITHIN GROUP(ORDER BY score)+0.25*percentile_cont(0.1) WITHIN GROUP(ORDER BY score))::numeric)::int score FROM scored`;
    if(!row||!Number(row.analyzed_count||0))continue;
    const score=Number(row.score),band=c6Band(score);
    await sql`INSERT INTO piko_quality_aggregates(entity_type,entity_key,parent_key,season_index,score,band,analyzed_count,total_count,formula_version,updated_at) VALUES('show',${show},NULL,NULL,${score},${band},${Number(row.analyzed_count)},${Number(row.total_count||0)},${PIKOQUALITY_C6_VERSION},now()) ON CONFLICT(entity_type,entity_key) DO UPDATE SET parent_key=EXCLUDED.parent_key,season_index=EXCLUDED.season_index,score=EXCLUDED.score,band=EXCLUDED.band,analyzed_count=EXCLUDED.analyzed_count,total_count=EXCLUDED.total_count,formula_version=EXCLUDED.formula_version,updated_at=now()`;
    rebuiltShows++;
  }
  return{seasons,shows:rebuiltShows};
}

export async function scorePikoQualityRatingKeys(sql,ratingKeys=[]){
  const keys=[...new Set((ratingKeys||[]).map(String).filter(Boolean))];
  if(!keys.length)return{processed:0,version:PIKOQUALITY_C6_VERSION,aggregates:{seasons:0,shows:0}};
  const rows=await sql`SELECT p.rating_key,p.item_type,p.plex_year,s.technical_fingerprint,m.width,m.height,m.bitrate,m.video_codec,m.video_profile,f.file_size_bytes,f.duration_ms,vs.bit_depth,COALESCE(vs.bitrate,m.bitrate) video_bitrate,aus.codec audio_codec,aus.channels audio_channels,aus.bitrate audio_bitrate FROM plex_items p JOIN plex_technical_state s ON s.rating_key=p.rating_key AND s.snapshot_status='ready' AND s.technical_fingerprint IS NOT NULL LEFT JOIN LATERAL(SELECT pm.* FROM plex_media pm WHERE pm.rating_key=p.rating_key ORDER BY pm.bitrate DESC NULLS LAST,pm.media_index LIMIT 1)m ON true LEFT JOIN LATERAL(SELECT pf.* FROM plex_files pf WHERE pf.rating_key=p.rating_key ORDER BY pf.file_size_bytes DESC NULLS LAST,pf.media_index,pf.part_index LIMIT 1)f ON true LEFT JOIN LATERAL(SELECT ps.bit_depth,ps.bitrate FROM plex_streams ps WHERE ps.rating_key=p.rating_key AND ps.stream_type=1 ORDER BY ps.bitrate DESC NULLS LAST,ps.stream_index LIMIT 1)vs ON true LEFT JOIN LATERAL(SELECT ps.codec,ps.channels,ps.bitrate FROM plex_streams ps WHERE ps.rating_key=p.rating_key AND ps.stream_type=2 ORDER BY ps.channels DESC NULLS LAST,ps.bitrate DESC NULLS LAST,ps.stream_index LIMIT 1)aus ON true WHERE p.active AND p.item_type IN('movie','episode') AND p.rating_key=ANY(${keys})`;
  if(!rows.length)return{processed:0,version:PIKOQUALITY_C6_VERSION,aggregates:{seasons:0,shows:0}};
  const payload=rows.map(r=>{const x=scorePikoQualityC6(r);return{rating_key:String(r.rating_key),item_type:r.item_type,score:x.scoreCompat,band:x.band,confidence:'high',formula_version:PIKOQUALITY_C6_VERSION,status:'evaluated',source_fingerprint:r.technical_fingerprint,components:x.components}});
  await sql`WITH x AS (SELECT * FROM jsonb_to_recordset(${JSON.stringify(payload)}::jsonb) AS t(rating_key text,item_type text,score int,band text,confidence text,formula_version text,status text,source_fingerprint text,components jsonb)) INSERT INTO piko_quality(rating_key,item_type,score,band,confidence,formula_version,status,source_fingerprint,components,enriched_at,last_error,evaluated_at,updated_at) SELECT rating_key,item_type,score,band,confidence,formula_version,status,source_fingerprint,components,now(),NULL,now(),now() FROM x ON CONFLICT(rating_key) DO UPDATE SET item_type=EXCLUDED.item_type,score=EXCLUDED.score,band=EXCLUDED.band,confidence=EXCLUDED.confidence,formula_version=EXCLUDED.formula_version,status='evaluated',source_fingerprint=EXCLUDED.source_fingerprint,components=EXCLUDED.components,enriched_at=now(),last_error=NULL,evaluated_at=now(),updated_at=now()`;
  const scoredKeys=payload.map(x=>x.rating_key),aggregates=await rebuildAffectedAggregates(sql,scoredKeys);
  return{processed:payload.length,version:PIKOQUALITY_C6_VERSION,ratingKeys:scoredKeys,aggregates};
}
