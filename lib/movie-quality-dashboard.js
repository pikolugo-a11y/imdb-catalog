import 'server-only';
import {db} from './db';
import {getMovieQualitySettings} from './movie-quality-settings';

export async function getMovieQualityDashboard(filters={}){
  const sql=db(),q=String(filters.q||'').trim().toLowerCase(),type=String(filters.type||''),risk=String(filters.risk||''),page=Math.max(1,Number(filters.page)||1),pageSize=10,offset=(page-1)*pageSize;
  const rows=await sql`SELECT f.id,f.finding_type,f.severity,f.risk_score,f.status,f.details,f.first_seen_at,f.last_seen_at,f.imdb_id,p.rating_key,p.plex_title,p.plex_year,pm.resolution,pm.bitrate,pm.width,pm.height,pm.video_codec,pm.audio_codec,pm.audio_channels,pf.file_path,pf.file_size_bytes,c.poster_path,pq.score pikoquality_score,pq.band pikoquality_band,count(*) OVER()::int total_count FROM movie_quality_findings f JOIN catalog_lifecycle cl ON cl.imdb_id=f.imdb_id AND cl.lifecycle_state='MOVIE_FILE_REVIEW' JOIN plex_items p ON p.rating_key=f.rating_key AND p.active LEFT JOIN LATERAL(SELECT * FROM plex_media z WHERE z.rating_key=p.rating_key ORDER BY media_index LIMIT 1)pm ON true LEFT JOIN LATERAL(SELECT * FROM plex_files z WHERE z.rating_key=p.rating_key ORDER BY media_index,part_index LIMIT 1)pf ON true LEFT JOIN catalog_read_model c ON c.imdb_id=f.imdb_id LEFT JOIN piko_quality pq ON pq.rating_key=f.rating_key WHERE f.status='pending' AND f.finding_type IN('duration','filename','duplicate') AND (${type}='' OR f.finding_type=${type}) AND (${risk}='' OR (${risk}='high' AND f.risk_score>=65)) AND (${q}='' OR lower(COALESCE(p.plex_title,'')) LIKE ${'%'+q+'%'}) ORDER BY f.risk_score DESC,f.last_seen_at DESC LIMIT ${pageSize} OFFSET ${offset}`;
  const total=rows[0]?.total_count||0;
  const [summary]=await sql`SELECT count(*)::int pending,count(DISTINCT f.rating_key)::int affected,count(*) FILTER(WHERE f.risk_score>=65)::int priority,max(f.last_seen_at) last_analysis FROM movie_quality_findings f JOIN catalog_lifecycle cl ON cl.imdb_id=f.imdb_id AND cl.lifecycle_state='MOVIE_FILE_REVIEW' WHERE f.status='pending' AND f.finding_type IN('duration','filename','duplicate')`;
  const types=await sql`SELECT f.finding_type,count(*)::int total,count(DISTINCT f.rating_key)::int affected FROM movie_quality_findings f JOIN catalog_lifecycle cl ON cl.imdb_id=f.imdb_id AND cl.lifecycle_state='MOVIE_FILE_REVIEW' WHERE f.status='pending' AND f.finding_type IN('duration','filename','duplicate') GROUP BY f.finding_type ORDER BY f.finding_type`;
  const settings=await getMovieQualitySettings();
  return{rows:rows.map(({total_count,...r})=>r),summary:{...(summary||{}),types},settings,total,page,pages:Math.max(1,Math.ceil(total/pageSize)),pageSize};
}
