import 'server-only';import{db}from'./db';export async function getExcludedTitles(){const sql=db();return sql`SELECT m.imdb_id,COALESCE(m.title_es,m.title,m.original_title) display_title,m.year,m.type,m.poster_path,ce.reason,ce.excluded_at FROM catalog_exclusions ce JOIN movies m USING(imdb_id) ORDER BY ce.excluded_at DESC LIMIT 300`}
