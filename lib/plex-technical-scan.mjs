import {discoverPlexUrlCore} from './plex-sync-core.mjs';
import {buildPhysicalIdentity,buildProbeFingerprint} from './plex-technical-core.mjs';

const CLIENT='pikofilm-technical-scan';
const headers=token=>({'Accept':'application/json','X-Plex-Token':token,'X-Plex-Client-Identifier':CLIENT,'X-Plex-Product':'PikoFilm','X-Plex-Version':'2'});
const list=body=>body?.MediaContainer?.Metadata||body?.MediaContainer?.Video||body?.MediaContainer?.Directory||[];
const num=v=>{const n=Number(v);return Number.isFinite(n)?n:null};
const ts=v=>v?new Date(Number(v)*1000).toISOString():null;
const canonicalPhysical=parts=>(Array.isArray(parts)?parts:[]).map(p=>({id:p?.id==null?null:String(p.id),file:p?.file==null?null:String(p.file),size:p?.size==null?null:Number(p.size),duration:p?.duration==null?null:Number(p.duration)})).sort((a,b)=>String(a.id||'').localeCompare(String(b.id||''))||String(a.file||'').localeCompare(String(b.file||'')));
const samePhysical=(a,b)=>JSON.stringify(canonicalPhysical(a))===JSON.stringify(canonicalPhysical(b));
async function pget(base,token,path){const r=await fetch(base+path,{headers:headers(token),cache:'no-store',signal:AbortSignal.timeout(60000)});if(!r.ok)throw new Error(`Plex ${path} respondió ${r.status}`);return r.json()}
async function listSections(base,token){const body=await pget(base,token,'/library/sections');return list(body).filter(x=>x.type==='movie'||x.type==='show').map(x=>({key:String(x.key),type:x.type,title:x.title||null}))}
function probeRow(item,section){return{rating_key:String(item.ratingKey),library_section_id:Number(section.key),item_type:item.type==='episode'?'episode':'movie',plex_key:item.key||null,plex_title:item.title||null,plex_year:num(item.year),parent_rating_key:item.parentRatingKey?String(item.parentRatingKey):null,grandparent_rating_key:item.grandparentRatingKey?String(item.grandparentRatingKey):null,parent_index:num(item.parentIndex),item_index:num(item.index),added_at:ts(item.addedAt),physical_identity:buildPhysicalIdentity(item),probe_fingerprint:buildProbeFingerprint(item)}}
async function ensurePhysicalItems(sql,rows){if(!rows.length)return 0;const payload=JSON.stringify(rows);const inserted=await sql`WITH x AS (SELECT * FROM jsonb_to_recordset(${payload}::jsonb) AS t(rating_key text,library_section_id int,item_type text,plex_key text,plex_title text,plex_year int,parent_rating_key text,grandparent_rating_key text,parent_index int,item_index int,added_at timestamptz)) INSERT INTO plex_items(rating_key,library_section_id,plex_key,plex_title,plex_year,added_at,item_type,parent_rating_key,grandparent_rating_key,parent_index,item_index,active,first_seen_at,last_seen_at,synced_at) SELECT rating_key,library_section_id,plex_key,plex_title,plex_year,added_at,item_type,parent_rating_key,grandparent_rating_key,parent_index,item_index,true,now(),now(),now() FROM x ON CONFLICT(rating_key) DO NOTHING RETURNING rating_key`;return inserted.length}
async function upsertProbeChunk(sql,rows){if(!rows.length)return{created:0,changed:0,migrated:0};await ensurePhysicalItems(sql,rows);const payload=JSON.stringify(rows);const existing=await sql`WITH x AS (SELECT * FROM jsonb_to_recordset(${payload}::jsonb) AS t(rating_key text)) SELECT s.rating_key,s.probe_fingerprint,s.technical_fingerprint,s.snapshot_status,COALESCE((SELECT jsonb_agg(jsonb_build_object('id',f.plex_part_id,'file',f.file_path,'size',f.file_size_bytes,'duration',f.duration_ms) ORDER BY f.media_index,f.part_index) FROM plex_files f WHERE f.rating_key=s.rating_key),'[]'::jsonb) AS physical_identity FROM plex_technical_state s JOIN x USING(rating_key)`;const old=new Map(existing.map(r=>[String(r.rating_key),r]));const createdKeys=[],changedKeys=[],migratedKeys=[];for(const r of rows){const prev=old.get(r.rating_key);if(!prev){createdKeys.push(r.rating_key);continue}if(prev.probe_fingerprint===r.probe_fingerprint)continue;const oldPhysical=canonicalPhysical(prev.physical_identity);const physicalChanged=oldPhysical.length>0?!samePhysical(oldPhysical,r.physical_identity):prev.snapshot_status!=='error';if(physicalChanged)changedKeys.push(r.rating_key);else migratedKeys.push(r.rating_key)}await sql`WITH x AS (SELECT * FROM jsonb_to_recordset(${payload}::jsonb) AS t(rating_key text,probe_fingerprint text)) INSERT INTO plex_technical_state(rating_key,probe_fingerprint,technical_fingerprint,snapshot_status,snapshot_version,needs_refresh,captured_at,source_updated_at,last_probe_at,last_error,updated_at) SELECT rating_key,probe_fingerprint,NULL,'pending','1',true,NULL,NULL,now(),NULL,now() FROM x ON CONFLICT(rating_key) DO UPDATE SET probe_fingerprint=EXCLUDED.probe_fingerprint,last_probe_at=now(),updated_at=now()`;if(changedKeys.length){await sql`UPDATE plex_technical_state SET snapshot_status=CASE WHEN technical_fingerprint IS NULL THEN 'pending' ELSE 'stale' END,needs_refresh=true,last_error=NULL,updated_at=now() WHERE rating_key=ANY(${changedKeys})`;await sql`UPDATE piko_quality SET status='stale',updated_at=now() WHERE rating_key=ANY(${changedKeys}) AND status='evaluated'`}return{created:createdKeys.length,changed:changedKeys.length,migrated:migratedKeys.length}}
async function scanSectionPaged({sql,base,token,section,pageSize=2000,onPage}){const started=Date.now();let start=0,total=0,created=0,changed=0,migrated=0,pages=0;const typeParam=section.type==='show'?'&type=4':'';for(;;){const path=`/library/sections/${encodeURIComponent(section.key)}/all?includeMedia=1${typeParam}&X-Plex-Container-Start=${start}&X-Plex-Container-Size=${pageSize}`;const body=await pget(base,token,path);const container=body?.MediaContainer||{};const expected=Number(container.totalSize||container.size||0);const items=list(body).filter(x=>section.type==='movie'?x.type==='movie':x.type==='episode');const rows=items.map(item=>probeRow(item,section));const result=await upsertProbeChunk(sql,rows);total+=rows.length;created+=result.created;changed+=result.changed;migrated+=result.migrated;pages++;start+=items.length;if(onPage)await onPage({section:section.title,type:section.type,total,created,changed,migrated,pages,expected,elapsed_ms:Date.now()-started});if(items.length===0||items.length<pageSize||(expected>0&&start>=expected))break}const elapsedMs=Date.now()-started;return{total,created,changed,migrated,pages,elapsed_ms:elapsedMs,items_per_second:elapsedMs?Number((total/(elapsedMs/1000)).toFixed(1)):0}}
export async function scanPlexTechnicalLibrary({sql,token,baseUrl='',pageSize=2000,onProgress}){if(!token)throw new Error('PLEX_TOKEN no está configurado');const started=Date.now();const base=await discoverPlexUrlCore(token,baseUrl);const sections=await listSections(base,token);let movies=0,episodes=0,changed=0,created=0,migrated=0,pages=0;const section_stats=[];for(const section of sections){const baseTotals={movies,episodes,changed,created,migrated,pages};const result=await scanSectionPaged({sql,base,token,section,pageSize,onPage:async p=>{const currentMovies=baseTotals.movies+(section.type==='movie'?p.total:0);const currentEpisodes=baseTotals.episodes+(section.type==='show'?p.total:0);const currentTotal=currentMovies+currentEpisodes;const elapsedMs=Date.now()-started;if(onProgress)await onProgress({phase:'scan',section:p.section,section_type:p.type,movies:currentMovies,episodes:currentEpisodes,total:currentTotal,created:baseTotals.created+p.created,changed:baseTotals.changed+p.changed,migrated:baseTotals.migrated+p.migrated,pages:baseTotals.pages+p.pages,elapsed_ms:elapsedMs,items_per_second:elapsedMs?Number((currentTotal/(elapsedMs/1000)).toFixed(1)):0})}});if(section.type==='movie')movies+=result.total;else episodes+=result.total;created+=result.created;changed+=result.changed;migrated+=result.migrated;pages+=result.pages;section_stats.push({section:section.title,type:section.type,...result})}const total=movies+episodes;const elapsedMs=Date.now()-started;return{sections:sections.length,movies,episodes,total,changed,created,migrated,pages,page_size:pageSize,elapsed_ms:elapsedMs,items_per_second:elapsedMs?Number((total/(elapsedMs/1000)).toFixed(1)):0,section_stats}}
