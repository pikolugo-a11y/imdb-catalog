import {reconcileSeriesReferencesFromPlexCore} from './series-reference-reconcile-core.mjs';
import {rebuildSeriesDiagnostics} from './series-diagnostics-core.mjs';
import {nextReferenceCheck} from './series-quality-domain.mjs';
import {recomputeLifecycleWithSql} from './lifecycle-recompute-core.mjs';
const API='https://api.themoviedb.org/3',today=()=>new Date().toISOString().slice(0,10),sleep=ms=>new Promise(r=>setTimeout(r,ms));
async function tmdb(path,{trace=null,apiGate=null,lane='manual',attempt=0}={}){const token=process.env.TMDB_API_TOKEN;if(!token)throw new Error('TMDB_API_TOKEN no configurado');let permit=null;if(apiGate)permit=await apiGate.acquire('tmdb',{lane});trace?.externalCall?.(1);try{const r=await fetch(`${API}${path}`,{headers:{Authorization:`Bearer ${token}`,Accept:'application/json'},cache:'no-store'});if(r.ok){await apiGate?.success?.('tmdb',{lane,permit});return r.json()}if((r.status===429||r.status>=500)&&attempt<3){await apiGate?.failure?.('tmdb',{lane,permit,status:r.status,retryable:true});await trace?.event?.({eventType:'retry',step:'tmdb_request',message:`TMDb ${r.status}; reintento ${attempt+1}`,data:{status:r.status,attempt:attempt+1}});await sleep(Number(r.headers.get('retry-after')||0)*1000||500*(2**attempt));return tmdb(path,{trace,apiGate,lane,attempt:attempt+1})}await apiGate?.failure?.('tmdb',{lane,permit,status:r.status,retryable:r.status===429||r.status>=500});throw Object.assign(new Error(`TMDb respondió ${r.status}`),{source:'tmdb',retryable:r.status===429||r.status>=500,status:r.status})}catch(e){if(!e?.status)await apiGate?.failure?.('tmdb',{lane,permit,retryable:true}).catch(()=>{});throw e}finally{await apiGate?.release?.('tmdb',{lane,permit}).catch(()=>{})}}
export async function refreshSeriesUnitaryCanonical(sql,{imdbId=null,ratingKey=null,trace=null,apiGate=null,lane='manual'}={}){if(!imdbId&&!ratingKey)throw new Error('Falta identificar la serie');await trace?.event?.({eventType:'step_started',step:'reconcile_reference',message:'Reconciliando referencia de serie desde Plex'});await reconcileSeriesReferencesFromPlexCore(sql);await trace?.event?.({eventType:'step_completed',step:'reconcile_reference',message:'Referencia Plex reconciliada'});const[s]=ratingKey?await sql`SELECT r.show_rating_key,r.imdb_id,r.tmdb_id FROM series_reference r JOIN identity_validation iv ON iv.imdb_id=r.imdb_id AND iv.validation_status='valid' LEFT JOIN catalog_exclusions ex ON ex.imdb_id=r.imdb_id WHERE r.show_rating_key=${String(ratingKey)} AND ex.imdb_id IS NULL LIMIT 1`:await sql`SELECT r.show_rating_key,r.imdb_id,r.tmdb_id FROM series_reference r JOIN identity_validation iv ON iv.imdb_id=r.imdb_id AND iv.validation_status='valid' LEFT JOIN catalog_exclusions ex ON ex.imdb_id=r.imdb_id WHERE r.imdb_id=${String(imdbId)} AND ex.imdb_id IS NULL LIMIT 1`;if(!s)throw new Error('La serie no está preparada para esta fase');if(!s.tmdb_id)throw new Error('La serie no tiene TMDb ID');const d=await tmdb(`/tv/${s.tmdb_id}?language=es-ES&append_to_response=watch/providers`,{trace,apiGate,lane}),providers=d['watch/providers']?.results?.ES,hasEs=Boolean(providers&&(providers.flatrate?.length||providers.buy?.length||providers.rent?.length||providers.free?.length||providers.ads?.length)),seasons=(d.seasons||[]).filter(x=>x.season_number>0),episodes=[];for(const season of seasons){const sd=await tmdb(`/tv/${s.tmdb_id}/season/${season.season_number}?language=es-ES`,{trace,apiGate,lane}),arr=sd.episodes||[],air=arr.map(x=>x.air_date).filter(Boolean).sort();let status='UNKNOWN',confidence='unknown';if(air.length&&air[0]>today()){status='ES_NOT_YET';confidence='high'}const note=status==='ES_NOT_YET'?'Temporada todavía no estrenada':hasEs?'Proveedor ES detectado para la serie, pero no demuestra disponibilidad de esta temporada':'Sin evidencia suficiente de distribución ES de esta temporada';await sql`INSERT INTO series_season_availability(show_rating_key,season_number,country_code,status,available_from,source,confidence,manual_override,checked_at,note) VALUES(${s.show_rating_key},${season.season_number},'ES',${status},${air[0]||null},'tmdb_watch_providers',${confidence},false,now(),${note}) ON CONFLICT(show_rating_key,season_number,country_code) DO UPDATE SET status=CASE WHEN series_season_availability.manual_override OR series_season_availability.status='PLEX_COMPLETE' OR (series_season_availability.status IN('ES_AVAILABLE','ES_PARTIAL') AND series_season_availability.source IN('tmdb_season_watch_providers','watchmode_episode_witness_es')) THEN series_season_availability.status ELSE EXCLUDED.status END,available_from=CASE WHEN series_season_availability.manual_override OR series_season_availability.status='PLEX_COMPLETE' OR (series_season_availability.status IN('ES_AVAILABLE','ES_PARTIAL') AND series_season_availability.source IN('tmdb_season_watch_providers','watchmode_episode_witness_es')) THEN series_season_availability.available_from ELSE EXCLUDED.available_from END,source=CASE WHEN series_season_availability.manual_override OR series_season_availability.status='PLEX_COMPLETE' OR (series_season_availability.status IN('ES_AVAILABLE','ES_PARTIAL') AND series_season_availability.source IN('tmdb_season_watch_providers','watchmode_episode_witness_es')) THEN series_season_availability.source ELSE EXCLUDED.source END,confidence=CASE WHEN series_season_availability.manual_override OR series_season_availability.status='PLEX_COMPLETE' OR (series_season_availability.status IN('ES_AVAILABLE','ES_PARTIAL') AND series_season_availability.source IN('tmdb_season_watch_providers','watchmode_episode_witness_es')) THEN series_season_availability.confidence ELSE EXCLUDED.confidence END,checked_at=now(),note=CASE WHEN series_season_availability.manual_override OR series_season_availability.status='PLEX_COMPLETE' OR (series_season_availability.status IN('ES_AVAILABLE','ES_PARTIAL') AND series_season_availability.source IN('tmdb_season_watch_providers','watchmode_episode_witness_es')) THEN series_season_availability.note ELSE EXCLUDED.note END`;for(const e of arr)episodes.push({show_rating_key:s.show_rating_key,season_number:season.season_number,episode_number:e.episode_number,tmdb_episode_id:String(e.id),name:e.name||null,overview:e.overview||null,air_date:e.air_date||null,runtime_minutes:e.runtime||null});await trace?.event?.({eventType:'step_completed',step:'tmdb_season',entityType:'season',entityId:`${s.show_rating_key}:${season.season_number}`,message:`Temporada ${season.season_number}: ${arr.length} episodios`,data:{episodes:arr.length,status}})}await sql`DELETE FROM series_reference_episodes WHERE show_rating_key=${s.show_rating_key}`;for(let i=0;i<episodes.length;i+=200){const payload=JSON.stringify(episodes.slice(i,i+200));await sql`INSERT INTO series_reference_episodes(show_rating_key,season_number,episode_number,tmdb_episode_id,name,overview,air_date,runtime_minutes) SELECT x.show_rating_key,x.season_number,x.episode_number,x.tmdb_episode_id,x.name,x.overview,x.air_date::date,x.runtime_minutes FROM jsonb_to_recordset(${payload}::jsonb) AS x(show_rating_key text,season_number int,episode_number int,tmdb_episode_id text,name text,overview text,air_date text,runtime_minutes int)`}const tmdbStatus=d.status||null,firstAir=d.first_air_date||null,lastAir=d.last_air_date||d.last_episode_to_air?.air_date||null,nextAir=d.next_episode_to_air?.air_date||null,next=nextReferenceCheck({status:tmdbStatus,lastAirDate:lastAir,nextAirDate:nextAir,refreshedAt:new Date()});await sql`UPDATE series_reference SET title=COALESCE(${d.name||null},title),original_title=COALESCE(${d.original_name||null},original_title),year=COALESCE(${Number(String(d.first_air_date||'').slice(0,4))||null},year),official_seasons=${seasons.length},official_episodes=${Number(d.number_of_episodes||episodes.length||0)},reference_source='tmdb',tmdb_status=${tmdbStatus},first_air_date=${firstAir},last_air_date=${lastAir},next_air_date=${nextAir},refreshed_at=now(),next_check_at=${next.toISOString()},plex_invalidated_at=NULL,plex_invalid_reason=NULL WHERE show_rating_key=${s.show_rating_key}`;const diagnostics=await rebuildSeriesDiagnostics(sql,s.show_rating_key),lifecycle=(await recomputeLifecycleWithSql(sql,[s.imdb_id])).get(s.imdb_id);return{ok:true,imdbId:s.imdb_id,ratingKey:s.show_rating_key,seasons:seasons.length,episodes:episodes.length,tmdbStatus,nextCheckAt:next.toISOString(),diagnostics,lifecycle}}
