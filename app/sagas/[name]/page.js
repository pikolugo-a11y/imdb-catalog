import Link from 'next/link';import {notFound}from'next/navigation';import{getSagaDetail}from'@/lib/queries';import{MediaCard}from'@/components/MediaCard';export const dynamic='force-dynamic';export default async function Saga({params}){const{name}=await params;const title=decodeURIComponent(name);const rows=await getSagaDetail(title);if(!rows.length)notFound();const inPlex=rows.filter(x=>x.effective_status==='in_plex').length,acquiring=rows.filter(x=>x.effective_status==='acquiring').length,pct=Math.round(inPlex/rows.length*100);return <><div className="breadcrumbs"><Link href="/sagas">Sagas</Link><span>›</span><b>{title}</b></div><div className="page-head"><div><div className="eyebrow">Colección</div><h1>{title}</h1><p>{rows.length} seleccionadas · {inPlex} en Plex · {acquiring} en proceso · {rows.length-inPlex-acquiring} faltan</p></div><div className="score"><span>{pct}%</span><small>completitud</small></div></div><div className="media-grid">{rows.map(r=><MediaCard key={r.imdb_id} item={r} href={`/catalogo/${r.imdb_id}`}/>)}</div></>}
