import Link from 'next/link';
import { notFound } from 'next/navigation';
import { getSagaDetail } from '@/lib/queries';
import { Status } from '@/components/Status';
export const dynamic='force-dynamic';
export default async function Saga({params}){const{name}=await params;const title=decodeURIComponent(name);const rows=await getSagaDetail(title);if(!rows.length)notFound();const inPlex=rows.filter(x=>x.effective_status==='in_plex').length,acquiring=rows.filter(x=>x.effective_status==='acquiring').length,pct=Math.round(inPlex/rows.length*100);return <><Link className="back" href="/sagas">← Volver a sagas</Link><div className="detail-head"><div><div className="eyebrow">Colección</div><h1>{title}</h1><p className="original">{rows.length} títulos seleccionados · {inPlex} en Plex · {acquiring} en proceso</p></div><div className="score"><span>{pct}%</span><small>completitud</small></div></div><div className="table-wrap"><table><thead><tr><th>Título</th><th>Año</th><th>PikoScore</th><th>Estado</th><th>Resolución</th></tr></thead><tbody>{rows.map(r=><tr key={r.imdb_id}><td><Link className="title" href={`/catalogo/${r.imdb_id}`}>{r.display_title}</Link><span className="sub">{r.original_title||r.imdb_id}</span></td><td>{r.year||'—'}</td><td>{r.final_rating?.toFixed?.(2)??r.final_rating??'—'}</td><td><Status value={r.effective_status}/></td><td>{r.resolution||'—'}</td></tr>)}</tbody></table></div></>}
