import Link from 'next/link';import Kpi from '@/components/Kpi';import {MediaCard} from '@/components/MediaCard';import {getCatalog,getCatalogFilters,getCatalogStats} from '@/lib/queries';import {markAcquiring,excludeTitle} from '@/app/actions';
export const dynamic='force-dynamic';
function qs(p,patch={}){const x=new URLSearchParams();for(const[k,v]of Object.entries({...p,...patch}))if(v!==undefined&&v!==null&&v!=='')x.set(k,String(v));return x.toString()}
export default async function Catalogo({searchParams}){const p=await searchParams;const view=p.view==='list'?'list':'grid';const [rows,f,stats]=await Promise.all([getCatalog(p),getCatalogFilters(),getCatalogStats(p)]);const returnTo='/catalogo?'+qs(p);return <><div className="page-head"><div><div className="eyebrow">Selección</div><h1>Catálogo</h1><p>Decide qué incorporar a Plex. Los resultados y estadísticas responden a tus filtros.</p></div><div className="view-switch"><Link className={view==='grid'?'active':''} href={'/catalogo?'+qs(p,{view:'grid'})}>▦ Carátulas</Link><Link className={view==='list'?'active':''} href={'/catalogo?'+qs(p,{view:'list'})}>☷ Lista</Link></div></div><div className="mini-kpis"><Kpi label="Resultados" value={stats.total}/><Kpi label="En Plex" value={stats.in_plex}/><Kpi label="En proceso" value={stats.acquiring}/><Kpi label="Faltan" value={stats.missing}/></div><form className="filters ux-filters" method="get"><input type="hidden" name="view" value={view}/><input name="q" defaultValue={p.q||''} placeholder="Buscar película o serie…"/><select name="type" defaultValue={p.type||''}><option value="">Películas y series</option><option value="movie">Películas</option><option value="series">Series</option></select><select name="status" defaultValue={p.status||''}><option value="">Todos los estados</option><option value="missing">Falta</option><option value="acquiring">En proceso</option><option value="in_plex">En Plex</option></select><select name="genre" defaultValue={p.genre||''}><option value="">Todos los géneros</option>{f.genres.map(g=><option key={g}>{g}</option>)}</select><select name="year" defaultValue={p.year||''}><option value="">Todos los años</option>{f.years.map(y=><option key={y}>{y}</option>)}</select><button>Aplicar</button><Link href="/catalogo" className="filter-reset">Limpiar</Link></form><div className="result-line"><b>{stats.total.toLocaleString('es-ES')}</b> títulos <span>· mostrando los 150 primeros por valoración</span> · <Link href="/catalogo/excluidas">Ver excluidas</Link></div>{view==='grid'?<div className="media-grid">{rows.map(r=><div className="media-action" key={r.imdb_id}><MediaCard item={r} href={`/catalogo/${r.imdb_id}?from=${encodeURIComponent(returnTo)}`}/>{r.effective_status!=='in_plex'&&<div className="quick-actions"><form action={markAcquiring}><input type="hidden" name="imdbId" value={r.imdb_id}/><button>+ En proceso</button></form><form action={excludeTitle}><input type="hidden" name="imdbId" value={r.imdb_id}/><button className="ghost">Excluir</button></form></div>}</div>)}</div>:<div className="modern-list">{rows.map(r=><article key={r.imdb_id}><div className="list-main"><Link className="title" href={`/catalogo/${r.imdb_id}?from=${encodeURIComponent(returnTo)}`}>{r.display_title}</Link><span>{r.year||'—'} · {r.type==='Película'?'Película':'Serie'} · ⭐ {r.final_rating??'—'} · {(r.imdb_votes||0).toLocaleString('es-ES')} votos</span></div><div className="list-actions"><span className={`status ${r.effective_status==='in_plex'?'ok':'warn'}`}>{r.effective_status==='in_plex'?'En Plex':r.effective_status==='acquiring'?'En proceso':'Falta'}</span>{r.effective_status!=='in_plex'&&<><form action={markAcquiring}><input type="hidden" name="imdbId" value={r.imdb_id}/><button>En proceso</button></form><form action={excludeTitle}><input type="hidden" name="imdbId" value={r.imdb_id}/><button className="ghost">Excluir</button></form></>}</div></article>)}</div>}</>}
