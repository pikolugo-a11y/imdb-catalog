import Link from 'next/link';import {getPeopleBatchPreview} from '@/lib/people-batch';import {createPeopleBatchRunAction} from '../actions';export const dynamic='force-dynamic';export default async function PeopleBatch({searchParams}){const sp=await searchParams||{},limit=Math.max(1,Number(sp.limit)||5),mode=sp.mode==='all'?'all':'pending',p=await getPeopleBatchPreview({limit,mode});return <><div className="page-head batch-head"><div><div className="eyebrow">Batch / Personas</div><h1>Actualizar Personas</h1><p>Perfil, filmografía y clasificación PikoFilm. Railway procesa la cola y reutiliza la caché global de películas TMDb.</p></div><Link href="/admin/batch">← Batch</Link></div><section className="section compact-section"><div className="section-head"><div><h2>Nuevo run</h2><p>Solo procesa personas relevantes con más de 5 películas en el catálogo.</p></div></div><form method="get" className="batch-launch-form compact-launch"><label><span>Cantidad</span><input name="limit" type="number" min="1" max="5000" defaultValue={limit}/></label><label><span>Política</span><select name="mode" defaultValue={mode}><option value="pending">Solo pendientes / versión antigua</option><option value="all">Forzar todos</option></select></label><button>Previsualizar</button></form><div className="batch-preview compact-preview"><div className="batch-preview-main"><span>Entrarán</span><strong>{p.selected.length}</strong><small>{p.eligible} elegibles</small></div></div><form action={createPeopleBatchRunAction} className="batch-create-run compact-create"><input type="hidden" name="limit" value={limit}/><input type="hidden" name="peopleMode" value={mode}/><button disabled={!p.selected.length}>Crear run de {p.selected.length}</button><span>Concurrencia inicial Railway: 4 personas · caché TMDb compartida</span></form><details className="batch-fold" open><summary>Personas preseleccionadas ({p.selected.length})</summary><div className="table-wrap"><table><thead><tr><th>Persona</th><th>Créditos catálogo</th><th>Filmografía</th><th>Versión</th></tr></thead><tbody>{p.selected.map(x=><tr key={x.tmdb_person_id}><td><b>{x.name}</b><span className="sub">TMDb {x.tmdb_person_id}</span></td><td>{Math.max(Number(x.cast_n||0),Number(x.dir_n||0))}</td><td>{x.filmography_refreshed_at?'Actualizada':'Pendiente'}</td><td>{x.version||0}</td></tr>)}</tbody></table></div></details></section><section className="section compact-section"><h2>Qué hace este proceso</h2><p>Actualiza datos biográficos y filmografía, consulta cada película una sola vez gracias a <b>tmdb_movie_cache</b>, clasifica Filmografía PikoFilm / Otros créditos y guarda la versión de clasificación. Si Railway se reinicia, los jobs terminados permanecen terminados.</p></section></>}
