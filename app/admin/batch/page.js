import Link from 'next/link';
import {BATCH_STAGES,getBatchControlOverview} from '@/lib/batch-control';
import {getFlexibleBatchPreview,getBatchTiming,positiveBatchLimit} from '@/lib/batch-ui-metrics';
import {createBatchRunAction,previewBatchRunAction,pauseBatchAction,resumeBatchAction,pauseRunAction,resumeRunAction,cancelRunAction,updateSourceLimitAction,testSourceAction,closeSourceBreakerAction} from './actions';
import AutoRefresh from './AutoRefresh';
export const dynamic='force-dynamic';
const dt=v=>v?new Date(v).toLocaleString('es-ES'):'—';
const label=s=>({IDENTITY_PENDING:'Identidad',IDENTITY_VALIDATION:'Validación identidad',DATA_INCOMPLETE:'Datos principales',PIKOSCORE_PENDING:'PikoScore',MOVIE_FILE_PENDING:'Archivo película',SERIES_SYNC_PENDING:'Series',TECH_PENDING:'PikoQuality',PEOPLE_REFRESH:'Personas / Filmografías'}[s]||s);
const retryLabel=s=>({new_only:'Solo nuevos',new_and_technical:'Nuevos + errores técnicos',include_unresolved:'Incluir no resueltos',all:'Forzar todos'}[s]||s);
const outcomeClass=o=>o==='CORREGIDO'?'ok':o==='ERROR'||o==='REVISION_MANUAL'?'bad':['NO_ENCONTRADO','INCOMPLETO','SIN_CAMBIOS','ACTUALIZADO_SIN_AVANCE'].includes(o)?'warn':'';
const titleOf=j=>j.title_es||j.title||j.original_title||j.entity_id;const durationMs=(a,b)=>a&&b?Math.max(0,new Date(b)-new Date(a)):null;const fmtMs=ms=>{if(!Number.isFinite(Number(ms))||Number(ms)<0)return'—';const s=Math.round(Number(ms)/1000);if(s<60)return`${s} s`;const m=Math.floor(s/60),r=s%60;if(m<60)return`${m} min ${r?s%60+' s':''}`.trim();const h=Math.floor(m/60),rm=m%60;return`${h} h ${rm?rm+' min':''}`.trim()};
export default async function BatchAdmin({searchParams}){const sp=await searchParams||{},stage=BATCH_STAGES.includes(sp.stage)?sp.stage:'IDENTITY_PENDING',retry=['new_only','new_and_technical','include_unresolved','all'].includes(sp.retry)?sp.retry:'new_only',limit=positiveBatchLimit(sp.limit,25),d=await getBatchControlOverview(),paused=Boolean(d.control?.paused),active=d.activeRun,hasLive=Boolean(active&&['queued','running'].includes(active.status)),[preview,timing]=await Promise.all([getFlexibleBatchPreview({stage,retryMode:retry,limit}),getBatchTiming({stage,activeRunId:active?.id||null})]),activeProgress=active?`${active.done+active.failed+active.review+active.cancelled+active.skipped}/${active.jobs}`:'—',recentJobs=d.jobs.slice(0,50),recentRuns=d.runs.slice(0,12),selectedCount=Math.min(preview.eligible,limit),historicalAvg=timing.historical.avg_ms,estimatedTotal=historicalAvg?historicalAvg*selectedCount:null,liveAvg=timing.active?.avg_ms||historicalAvg,liveEta=liveAvg&&timing.active?liveAvg*timing.active.remaining:null,sourceTest=sp.sourceTest?{source:sp.sourceTest,ok:sp.sourceStatus==='ok',message:sp.sourceMessage||'',http:sp.sourceHttp||null,ms:sp.sourceMs||null}:null,isPeople=stage==='PEOPLE_REFRESH';return <><AutoRefresh active={hasLive}/><div className="page-head batch-head"><div><div className="eyebrow">Lifecycle Control Center</div><h1>Batch / Autopilot</h1><p>Todos los procesos masivos se lanzan desde el mismo control.</p></div><Link href="/admin">← Administración</Link></div><div className="batch-statusbar compact-bar"><div><span>Motor</span><strong className={paused?'status warn':'status ok'}>{paused?'PAUSADO':'ACTIVO'}</strong></div><div><span>Run</span><strong>{active?`#${active.id} · ${label(active.target_stage)}`:'Ninguno'}</strong></div><div><span>Progreso</span><strong>{activeProgress}</strong></div><div><span>Cola</span><strong>{d.summary.queued} · activos {d.summary.active}</strong></div><div className="batch-status-actions">{paused?<form action={resumeBatchAction}><button>Reanudar</button></form>:<form action={pauseBatchAction}><input type="hidden" name="reason" value="Pausado desde Lifecycle Control Center"/><button className="danger">Pausar</button></form>}</div></div>{active&&<section className="section compact-section batch-active"><div className="section-head"><div><h2>Run activo #{active.id}</h2><p>{label(active.target_stage)} · {retryLabel(active.limits?.retry_mode)} · {dt(active.started_at||active.created_at)}</p></div><strong>{activeProgress}</strong></div><div className="batch-outcomes compact-outcomes"><span className="status ok">Corregido {active.corrected}</span><span className="status warn">No encontrado {active.not_found}</span><span className="status warn">Incompleto {active.incomplete}</span><span>Sin cambios {active.no_change}</span><span className="status bad">Error {active.functional_error}</span><span className="status bad">Revisión {active.manual_review}</span></div><div className="batch-time-strip"><span><b>Media actual:</b> {liveAvg?fmtMs(liveAvg):'sin muestra suficiente'}/elemento</span><span><b>ETA:</b> {liveEta?fmtMs(liveEta):'—'}</span><span><b>Restantes:</b> {timing.active?.remaining??'—'}</span></div><div className="quick-actions">{['running','queued'].includes(active.status)&&<form action={pauseRunAction}><input type="hidden" name="runId" value={active.id}/><button>Pausar run</button></form>}{active.status==='paused'&&<form action={resumeRunAction}><input type="hidden" name="runId" value={active.id}/><button>Reanudar run</button></form>}<form action={cancelRunAction}><input type="hidden" name="runId" value={active.id}/><button className="ghost danger-soft">Cancelar</button></form></div></section>}<section className="section compact-section" id="launch"><div className="section-head"><div><h2>Lanzar lote</h2><p>{isPeople?'Actualiza perfiles y filmografías relevantes (>5 películas), usando la caché TMDb compartida.':'Previsualiza antes de ejecutar. El anti-bucle decide qué entra.'}</p></div></div><form action={previewBatchRunAction} className="batch-launch-form compact-launch"><label><span>Etapa</span><select name="stage" defaultValue={stage}>{BATCH_STAGES.map(s=><option key={s} value={s}>{label(s)}</option>)}</select></label><label><span>Cantidad</span><input name="limit" type="number" min="1" step="1" defaultValue={limit} inputMode="numeric"/></label><label><span>Política</span><select name="retryMode" defaultValue={retry}><option value="new_only">Solo nuevos</option><option value="new_and_technical">Nuevos + errores técnicos</option><option value="include_unresolved">Incluir no resueltos</option><option value="all">Forzar todos</option></select></label><button>Previsualizar</button></form><div className="batch-preview compact-preview"><div className="batch-preview-main"><span>Entrarán</span><strong>{selectedCount}</strong><small>{preview.eligible} elegibles · {preview.counts.total} en etapa</small></div><div className="batch-preview-grid"><span><b>{preview.counts.new}</b>Nuevos</span><span><b>{preview.counts.context_changed}</b>Cambio contexto</span><span><b>{preview.counts.technical_due}</b>Error retry</span><span><b>{preview.counts.unresolved}</b>No resueltos</span><span><b>{preview.counts.cooldown}</b>Cooldown</span><span><b>{preview.counts.manual_review}</b>Revisión</span></div></div><div className="batch-time-strip estimate"><span><b>Media histórica de {label(stage)}:</b> {historicalAvg?fmtMs(historicalAvg):'sin muestra suficiente'}/elemento</span><span><b>Muestra:</b> {timing.historical.samples} jobs</span><span><b>Tiempo estimado para {selectedCount}:</b> {estimatedTotal?fmtMs(estimatedTotal):'—'}</span></div><form action={createBatchRunAction} className="batch-create-run compact-create"><input type="hidden" name="stage" value={stage}/><input type="hidden" name="limit" value={limit}/><input type="hidden" name="retryMode" value={retry}/><button disabled={!preview.eligible}>Crear run de {selectedCount}</button><span>{retryLabel(retry)} · {paused?'quedará en espera':'se podrá ejecutar ya'}</span></form><details className="batch-fold"><summary>Ver {isPeople?'personas':'títulos'} preseleccionados ({preview.selected.length}{selectedCount>25?' de '+selectedCount:''})</summary><div className="table-wrap"><table><thead><tr><th>{isPeople?'Persona':'Título'}</th><th>{isPeople?'TMDb':'IMDb'}</th><th>Último resultado</th><th>Intentos/versión</th></tr></thead><tbody>{preview.selected.map(x=><tr key={x.imdb_id}><td><b>{x.title}</b><span className="sub">{x.year||'—'} · {x.type||'—'}</span></td><td>{x.imdb_id}</td><td>{x.last_outcome?<span className={`status ${outcomeClass(x.last_outcome)}`}>{x.last_outcome}</span>:'Nunca intentado'}</td><td>{x.attempt_count}</td></tr>)}</tbody></table></div></details></section><section className="section compact-section"><div className="section-head"><div><h2>Últimos jobs</h2><p>Una fila por elemento procesado.</p></div></div><div className="table-wrap"><table><thead><tr><th>Job</th><th>Elemento</th><th>Etapa</th><th>Resultado</th><th>Lifecycle</th><th>Tiempo</th><th></th></tr></thead><tbody>{recentJobs.map(j=><tr key={j.id}><td>#{j.id}</td><td><b>{titleOf(j)}</b><span className="sub">{j.entity_id} · {j.year||'—'}</span></td><td>{label(j.stage)}</td><td>{j.functional_outcome?<span className={`status ${outcomeClass(j.functional_outcome)}`}>{j.functional_outcome}</span>:j.status}</td><td>{j.lifecycle_before||'—'} → {j.lifecycle_after||'—'}</td><td>{fmtMs(durationMs(j.started_at,j.finished_at))}</td><td><Link className="title" href={`/admin/batch/job/${j.id}`}>Detalle →</Link></td></tr>)}</tbody></table></div></section><details className="section batch-fold"><summary><b>Historial de runs</b> · últimos {recentRuns.length}</summary><div className="table-wrap"><table><thead><tr><th>Run</th><th>Etapa</th><th>Estado</th><th>Jobs</th><th>Corregidos</th><th>No encontrados</th><th>Errores</th><th>Fecha</th></tr></thead><tbody>{recentRuns.map(r=><tr key={r.id}><td>#{r.id}</td><td>{label(r.target_stage)}</td><td>{r.status}</td><td>{r.jobs}</td><td>{r.corrected}</td><td>{r.not_found}</td><td>{r.functional_error}</td><td>{dt(r.created_at)}</td></tr>)}</tbody></table></div></details><details className="section batch-fold" id="sources" open={Boolean(sourceTest)}><summary><b>Fuentes, límites y breakers</b> · {d.limits.length} fuentes</summary>{sourceTest&&<div className={`batch-time-strip ${sourceTest.ok?'':'source-test-error'}`}><span><b>{sourceTest.source}:</b> {sourceTest.ok?'✅ Comunicación correcta':'❌ Prueba fallida'}</span><span>{sourceTest.message}</span></div>}<div className="source-grid compact-sources">{d.limits.map(x=><div className="source-card" key={x.source}><div><b>{x.source}</b><span className={`status ${x.enabled&&x.breaker_state!=='open'?'ok':'warn'}`}>{x.enabled?x.breaker_state:'deshabilitada'}</span></div><p>{x.usage?.attempts||0} intentos hoy · {x.usage?.failures||0} fallos · media {x.usage?.avg_ms??'—'} ms</p><div className="quick-actions"><form action={testSourceAction}><input type="hidden" name="source" value={x.source}/><button>Probar ahora</button></form>{x.breaker_state==='open'&&<form action={closeSourceBreakerAction}><input type="hidden" name="source" value={x.source}/><button className="ghost">Cerrar breaker</button></form>}</div><form action={updateSourceLimitAction} className="source-form"><input type="hidden" name="source" value={x.source}/><label><span>Presupuesto</span><input name="dailyBudget" type="number" min="0" defaultValue={x.daily_budget??''}/></label><label><span>Intervalo ms</span><input name="minIntervalMs" type="number" min="0" defaultValue={x.min_interval_ms??0}/></label><label><span>Concurrencia</span><input name="maxConcurrency" type="number" min="1" defaultValue={x.max_concurrency??1}/></label><label className="source-check"><input name="enabled" type="checkbox" defaultChecked={x.enabled}/><span>Activa</span></label><button>Guardar</button></form></div>)}</div></details></>}
