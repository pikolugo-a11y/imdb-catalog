import Link from 'next/link';
import Kpi from '@/components/Kpi';
import { getPlexOutsideCatalog,getPlexSummary } from '@/lib/queries';
export const dynamic='force-dynamic';
export default async function Plex({searchParams}){const p=await searchParams;const [s,rows]=await Promise.all([getPlexSummary(),getPlexOutsideCatalog(p)]);return <><div className="hero"><div><div className="eyebrow">Biblioteca real</div><h1>Plex</h1><p>Inventario real separado del catálogo deseado. El objetivo aquí es localizar cruces extraños y contenido que PikoFilm todavía no sabe contextualizar.</p></div></div><div className="grid"><Kpi label="Elementos activos" value={s.active_items}/><Kpi label="Cruces catálogo" value={s.catalog_matches}/><Kpi label="Fuera del catálogo" value={s.outside_catalog}/><Kpi label="Marcados vistos" value={s.watched_items}/></div><section className="section"><div className="section-head"><h2>Fuera del catálogo</h2><p>{s.last_sync?`Última sincronización correcta: ${new Date(s.last_sync).toLocaleString('es-ES')}`:'Sin sincronización registrada'}</p></div><form className="filters compact" method="get"><input name="q" defaultValue={p.q||''} placeholder="Buscar en Plex…"/><input name="year" defaultValue={p.year||''} inputMode="numeric" placeholder="Año"/><button>Filtrar</button><Link className="filter-reset" href="/plex">Limpiar</Link></form><div className="table-wrap"><table><thead><tr><th>Título Plex</th><th>Año</th><th>IMDb detectado</th><th>Estado cruce</th><th>Fecha alta</th></tr></thead><tbody>{rows.map(r=><tr key={r.rating_key}><td className="title">{r.plex_title||'Sin título'}<span className="sub">ratingKey {r.rating_key}</span></td><td>{r.plex_year||'—'}</td><td>{r.imdb_id||'—'}</td><td><span className="status warn">{r.catalog_state||'sin cruce'}</span></td><td>{r.added_at?new Date(r.added_at).toLocaleDateString('es-ES'):'—'}</td></tr>)}</tbody></table></div></section></>}
