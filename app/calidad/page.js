import Link from 'next/link';
import {getQualityDashboard} from '@/lib/quality-dashboard';
import {getDataQualityOverview} from '@/lib/data-quality';
import {getLifecycleSnapshot} from '@/lib/lifecycle';
import './quality-dashboard.css';
export const dynamic='force-dynamic';
const nf=n=>Number(n||0).toLocaleString('es-ES');
function Stat({tone='warn',label,value,sub,href}){return <Link href={href} className={`quality-stat q-${tone}`}><span>{label}</span><strong>{nf(value)}</strong><small>{sub}</small><em>Ver cola →</em></Link>}
export default async function Calidad(){const[d,dq,life]=await Promise.all([getQualityDashboard(),getDataQualityOverview(),getLifecycleSnapshot()]),c=life.counts||{},missing=Number(life.missing||0),identity=Number(c.IDENTITY_PENDING||0),validation=Number(c.IDENTITY_VALIDATION||0)+Number(c.IDENTITY_REVIEW_REQUIRED||0),data=Number(c.DATA_INCOMPLETE||0)+Number(c.PIKOSCORE_PENDING||0),series=Number(c.SERIES_SYNC_PENDING||0)+Number(c.SERIES_REVIEW||0),movies=Number(c.MOVIE_FILE_PENDING||0)+Number(c.MOVIE_FILE_REVIEW||0),tech=Number(c.TECH_PENDING||0),attention=missing+identity+validation+data+series+movies+tech;return <div className="quality-home"><div className="quality-title"><div><div className="eyebrow">Centro de control · ciclo de vida</div><h1>Calidad</h1><p>Catálogo es la fuente única. Aquí se muestran también los títulos que hayan quedado sin estado Lifecycle.</p></div></div><section className="quality-overview"><div className="quality-attention"><div className="attention-ring">!</div><div><strong>{nf(attention)}</strong><b>títulos requieren trabajo</b><span>Incluye incidencias de Lifecycle que necesitan recuperación.</span></div></div>{missing>0&&<Stat tone="warn" label="Sin estado" value={missing} sub="en catálogo pero fuera del Lifecycle" href="/calidad/sin-estado"/>}<Stat tone="identity" label="Identidad" value={identity} sub="faltan IDs necesarios" href="/calidad/identidad"/><Stat tone="identity" label="Validación" value={validation} sub="identidad pendiente/revisión" href="/calidad/validacion-identidad"/><Stat tone="warn" label="Datos" value={data} sub="datos o PikoScore pendientes" href="/calidad/datos"/><Stat tone="series" label="Series" value={series} sub="referencia o episodios" href="/calidad/series"/><Stat tone="movie" label="Películas" value={movies} sub="validación del archivo" href="/calidad/peliculas"/><Stat tone="movie" label="PikoQuality" value={tech} sub="calidad técnica pendiente" href="/calidad/pikoquality"/></section>{missing>0&&<section className="quality-actions"><article className="quality-action data"><div className="quality-action-main"><div className="quality-icon">!</div><div><h2>Recuperación Lifecycle <span>{nf(missing)} sin estado</span></h2><p>Estos títulos existen en Catálogo pero no tienen registro Lifecycle. Revísalos y reinicia únicamente los que corresponda.</p></div></div><div className="quality-action-buttons"><Link className="quality-review" href="/calidad/sin-estado">Abrir cola →</Link></div></article></section>}<section className="quality-pq"><div><div className="eyebrow">Estado final</div><h2>{nf(c.COMPLETE)} títulos completos</h2><p>{nf(life.materialized)} de {nf(life.total)} títulos tienen Lifecycle materializado.</p></div><Link className="quality-review" href="/catalogo">Ver Catálogo →</Link></section><div className="info-banner">Los títulos sin estado nunca se consideran completos: aparecen en Recuperación Lifecycle hasta que se recalculan.</div></div>}
