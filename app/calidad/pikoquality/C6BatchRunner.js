'use client';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
import {runC6BatchChunkAction} from './actions';
import styles from './pikoquality.module.css';
const nf=n=>Number(n||0).toLocaleString('es-ES');
const pct=(n,d)=>d?Math.round(Number(n||0)*1000/Number(d))/10:0;
const fmtMs=ms=>{const s=Math.max(0,Number(ms||0))/1000;return s<60?`${s.toFixed(1)} s`:`${Math.floor(s/60)}m ${Math.round(s%60)}s`};
export default function C6BatchRunner({initial}){const router=useRouter(),[running,setRunning]=useState(false),[current,setCurrent]=useState(Number(initial.current||0)),[remaining,setRemaining]=useState(Number(initial.pending||0)),[processed,setProcessed]=useState(0),[speed,setSpeed]=useState(0),[elapsed,setElapsed]=useState(0),[error,setError]=useState('');const total=Number(initial.total||0);async function runAll(){if(running||remaining<=0)return;setRunning(true);setError('');setProcessed(0);setSpeed(0);setElapsed(0);const started=performance.now();let left=remaining,done=0;try{while(left>0){const r=await runC6BatchChunkAction();done+=Number(r.processed||0);left=Number(r.remaining||0);const ms=performance.now()-started;setProcessed(done);setRemaining(left);setCurrent(total-left);setElapsed(ms);setSpeed(ms?Math.round(done/(ms/1000)):0);if(Number(r.processed||0)===0)break}router.refresh()}catch(e){setError(e?.message||'Error ejecutando PikoQuality C6')}finally{setRunning(false)}}const progress=pct(current,total);return <section className={styles.c6Panel}><div className={styles.syncHeader}><div><span className={styles.kicker}>PIKOQUALITY 2.0 · REGLA DE ORO</span><h2>Cálculo C6</h2><p>Batch interno sobre snapshots técnicos ya persistidos. No consulta Plex.</p></div><div className={styles.c6Version}>{initial.version}</div></div><div className={styles.c6ProgressHead}><span>Progreso C6 vigente</span><b>{nf(current)} / {nf(total)} · {progress}%</b></div><div className={styles.progressTrack}><span style={{width:`${Math.min(100,progress)}%`}}/></div><div className={styles.c6Stats}><div><span>Pendientes</span><strong>{nf(remaining)}</strong></div><div><span>Procesados esta ejecución</span><strong>{nf(processed)}</strong></div><div><span>Velocidad</span><strong>{running?`${nf(speed)} / s`:'—'}</strong></div><div><span>Tiempo</span><strong>{processed?fmtMs(elapsed):'—'}</strong></div></div><div className={styles.c6Meta}>Nunca C6: {nf(initial.neverC6)} · Archivo cambiado: {nf(initial.changed)} · Errores C6: {nf(initial.errors)}</div>{error?<div className={styles.syncError}>{error}</div>:null}<div className={styles.syncActions}><button className={styles.primaryButton} onClick={runAll} disabled={running||remaining<=0}>{running?'Calculando C6…':remaining>0?'Calcular pendientes C6':'PikoQuality C6 al día'}</button><div className={styles.syncActionNote}>{running?`Procesando en bloques internos de 10.000 · ${nf(remaining)} pendientes`:'Solo recalcula elementos sin C6 vigente o cuyo technical_fingerprint haya cambiado.'}</div></div></section>}
