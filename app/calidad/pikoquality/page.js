import Link from 'next/link';import {qualitySummary,backfillA,QUALITY_VERSION} from '../../../lib/pikoquality';import {enrichPending} from '../../../lib/pikoquality-enrichment';export const dynamic='force-dynamic';
export default async function Page({searchParams}){const sp=await searchParams;let result=null,error=null;if(sp?.action){try{if(sp.action==='backfill')result=await backfillA();if(sp.action==='enrich')result=await enrichPending(Math.min(200,Math.max(1,Number(sp.limit)||50)));}catch(e){error=String(e?.message||e)}}const s=await qualitySummary();return <><div className="hero"><div><div className="eyebrow">#44 · motor técnico</div><h1>PikoQuality</h1><p>Evaluación técnica persistente de películas y episodios. Fórmula {QUALITY_VERSION}. A calcula desde Neon; B enriquece únicamente pendientes desde Plex.</p></div><div className="actions"><Link className="btn" href="/calidad">← Calidad</Link></div></div>{result&&<div className="card"><strong>Proceso terminado.</strong> <span>{JSON.stringify(result)}</span></div>}{error&&<div className="card"><strong>Error:</strong> {error}</div>}<div className="stats-grid"><div className="stat"><span>Evaluados</span><strong>{s.evaluated||0}</strong></div><div className="stat"><span>Pendientes</span><strong>{s.pending||0}</strong></div><div className="stat"><span>Confianza alta (B)</span><strong>{s.high||0}</strong></div><div className="stat"><span>Stale / 404 Plex</span><strong>{s.stale||0}</strong></div></div><div className="card"><h2>1 · Carga / recálculo A</h2><p>Calcula PikoQuality usando exclusivamente los datos técnicos ya guardados en Neon. No consulta Plex. Úsalo una vez para la carga inicial y de nuevo solo si cambia la fórmula.</p><Link className="btn primary" href="/calidad/pikoquality?action=backfill">Ejecutar carga A</Link></div><div className="card"><h2>2 · Enriquecimiento B</h2><p>Consulta Plex solo para elementos todavía no enriquecidos o pendientes. Persiste Media → Part → Stream y recalcula la nota con confianza alta. Se ejecuta por lotes para no superar los límites del frontal.</p><div className="actions"><Link className="btn primary" href="/calidad/pikoquality?action=enrich&limit=50">Enriquecer 50 pendientes</Link><Link className="btn" href="/calidad/pikoquality?action=enrich&limit=200">Enriquecer 200 pendientes</Link></div><p className="muted">Repite B hasta que ya no procese elementos. Los 404 se marcan stale y no reciben una mala nota.</p></div><div className="card"><h2>Uso normal después del backfill</h2><p>No necesitas programar nada. Tras cambios de Plex, PikoQuality podrá reevaluar únicamente nuevos/cambiados. El recálculo global A queda como acción manual y B como enriquecimiento controlado desde esta pantalla.</p></div></>}
