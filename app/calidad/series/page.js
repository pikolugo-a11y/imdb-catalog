import Link from 'next/link';
import { getSeriesQuality } from '@/lib/queries';
export const dynamic='force-dynamic';
export default async function CalidadSeries({searchParams}){const p=await searchParams;const rows=await getSeriesQuality(p);return <><div className="hero"><div><div className="eyebrow">Diagnóstico · series</div><h1>Calidad de series</h1><p>Control episodio a episodio. Solo la disponibilidad confirmada en España convierte un hueco técnico en problema accionable.</p></div></div><form className="filters compact" method="get"><input name="q" defaultValue={p.q||''} placeholder="Buscar serie…"/><select name="state" defaultValue={p.state||'issues'}><option value="issues">Con incidencias</option><option value="actionable">Faltan en España</option><option value="unknown">Disponibilidad desconocida</option><option value="uptodate">Al día</option><option value="all">Todas</option></select><button>Filtrar</button><Link className="filter-reset" href="/calidad/series">Limpiar</Link></form><div className="alert">“Al día” significa que no existe ningún episodio confirmado como faltante en España ni pendiente de aclarar disponibilidad.</div><div className="table-wrap"><table><thead><tr><th>Serie</th><th>Temporadas</th><th>Presentes</th><th>Faltan ES</th><th>No disponibles</th><th>Desconocidos</th><th>Estado</th></tr></thead><tbody>{rows.map(r=>{const ok=!r.actionable_missing&&!r.availability_unknown;return <tr key={r.show_rating_key}><td><Link className="title" href={`/calidad/series/${r.show_rating_key}`}>{r.title}</Link><span className="sub">{r.year||'—'} · {r.official_episodes??'—'} episodios oficiales</span></td><td>{r.official_seasons??'—'}</td><td>{r.present}</td><td><span className={r.actionable_missing?'status bad':'status ok'}>{r.actionable_missing}</span></td><td>{r.not_yet_available}</td><td>{r.availability_unknown}</td><td><span className={`status ${ok?'ok':'warn'}`}>{ok?'Al día':'Revisar'}</span></td></tr>})}</tbody></table></div></>}
