import {neon} from '@neondatabase/serverless';
import crypto from 'node:crypto';

const dbUrl=process.env.DATABASE_URL;
const runId=Number(process.env.PIPELINE_RUN_ID||0);
const base=process.env.PIKOFILM_URL||'https://imdb-catalog-eight.vercel.app';
const runMode=process.env.IDENTITY_RUN_MODE||'full';
if(!dbUrl)throw new Error('Falta DATABASE_URL');
if(!runId)throw new Error('Falta PIPELINE_RUN_ID');
const sql=neon(dbUrl);
const token=crypto.createHash('sha256').update(dbUrl).digest('hex');
const started=Date.now();
const sleep=ms=>new Promise(r=>setTimeout(r,ms));

const counters=()=>({processed:0,updated:0,errors:0,notFound:0,wikiImdb:0,wikiTmdb:0,wikiRetry:0,pythonFa:0,pythonAmbiguous:0,pythonNotFound:0,pythonErrors:0,pythonBlocked:0,pythonCalls:0,braveFa:0,braveNoCandidate:0,braveBlocked:0});
async function audit(action,payload={}){try{await sql`INSERT INTO admin_events(event_type,entity_type,entity_id,action,payload,created_at) VALUES('identity','identity_refresh',${String(runId)},${action},${JSON.stringify(payload)}::jsonb,now())`}catch{}}
async function state(){const[r]=await sql`SELECT status,summary FROM pipeline_runs WHERE id=${runId}`;return r||null}
async function cancelled(){const r=await state();return !r||r.status!=='running'||Boolean(r.summary?.cancel_requested)}
function snapshot(c){return{found_total:c.updated,not_found:c.notFound,wikidata_imdb:c.wikiImdb,wikidata_tmdb:c.wikiTmdb,wikidata_retry:c.wikiRetry,python_fa:c.pythonFa,python_ambiguous:c.pythonAmbiguous,python_not_found:c.pythonNotFound,python_errors:c.pythonErrors,python_blocked:c.pythonBlocked,python_calls:c.pythonCalls,python_delay_ms:2500,brave_fa:c.braveFa,brave_no_candidate:c.braveNoCandidate,brave_blocked:c.braveBlocked}}
async function progress(c,total,block,bp,bt,current,extra={}){const pct=total?Math.min(99.9,Math.round(c.processed*1000/total)/10):0;await sql`UPDATE pipeline_runs SET processed_count=${c.processed},updated_count=${c.updated},error_count=${c.errors},summary=COALESCE(summary,'{}'::jsonb)||${JSON.stringify({stage:'running',progress_pct:pct,total_operations:total,current_block:block,block_processed:bp,block_total:bt,current_title:current||null,...snapshot(c),...extra})}::jsonb,updated_at=now() WHERE id=${runId}`}
async function finish(status,summary,c){await sql`UPDATE pipeline_runs SET status=${status},finished_at=now(),processed_count=${c.processed},updated_count=${c.updated},error_count=${c.errors},summary=COALESCE(summary,'{}'::jsonb)||${JSON.stringify(summary)}::jsonb,updated_at=now() WHERE id=${runId}`}
async function batch(ids,mode){for(let a=0;a<3;a++){const r=await fetch(`${base}/api/identity/batch`,{method:'POST',headers:{'content-type':'application/json','x-pikofilm-worker':token},body:JSON.stringify({runId,ids,mode})});if(r.ok)return r.json();if(r.status===409)return{cancelled:true};if(a<2){await sleep(1200*(a+1));continue}throw new Error(`Batch HTTP ${r.status}: ${(await r.text()).slice(0,200)}`)}}
function addStats(c,st={}){c.wikiImdb+=Number(st.wikidata_imdb||0);c.wikiTmdb+=Number(st.wikidata_tmdb||0);c.wikiRetry+=Number(st.wikidata_retry||0);c.pythonFa+=Number(st.python_fa||0);c.pythonAmbiguous+=Number(st.python_ambiguous||0);c.pythonNotFound+=Number(st.python_not_found||0);c.pythonErrors+=Number(st.python_errors||0);c.pythonBlocked+=Number(st.python_blocked||0);c.pythonCalls+=Number(st.python_calls||0);c.braveFa+=Number(st.brave_fa||0);c.braveNoCandidate+=Number(st.brave_no_candidate||0);c.braveBlocked+=Number(st.brave_blocked||0);c.notFound+=Number(st.not_found||0)}
async function phase(name,mode,rows,c,total){await audit('block_started',{block:name,total:rows.length,block_size:8});for(let i=0;i<rows.length;i+=8){if(await cancelled())return{ok:false,cancelled:true};const slice=rows.slice(i,i+8),res=await batch(slice.map(x=>x.imdb_id),mode);if(res?.cancelled)return{ok:false,cancelled:true};const st=res?.stats||{};addStats(c,st);for(const x of res.results||[]){c.processed++;if(x.ok&&x.updated)c.updated++;if(!x.ok){c.errors++;await audit('title_failed',{block:name,imdb_id:x.imdbId,error:x.error})}}await progress(c,total,name,Math.min(i+slice.length,rows.length),rows.length,slice.at(-1)?.title);if(Number(st.python_blocked||0)){const items=(res.results||[]).filter(x=>x?.python?.status==='blocked').map(x=>({imdb_id:x.imdbId,error:x.python.error,http_status:x.python.http_status}));await audit('fa_python_block_detected',{block:name,count:Number(st.python_blocked||0),items:items.slice(0,8)});return{ok:false,pythonBlocked:true}}}await audit('block_completed',{block:name,total:rows.length,...snapshot(c),errors:c.errors});return{ok:true}}

async function braveTest(){const rows=await sql`SELECT m.imdb_id,COALESCE(m.title_es,m.title,m.original_title) title FROM movies m LEFT JOIN catalog_exclusions ex USING(imdb_id) WHERE ex.imdb_id IS NULL AND m.imdb_id~'^tt[0-9]+$' AND m.fa_id IS NULL ORDER BY m.synced_at NULLS FIRST LIMIT 30`,total=rows.length,c=counters();await sql`UPDATE pipeline_runs SET source='github',summary=COALESCE(summary,'{}'::jsonb)||${JSON.stringify({stage:'running',scope:'brave_test_30',total_operations:total,total_unique:total,progress_pct:0,current_block:'Prueba Brave · 30 · 5 s',found_total:0,not_found:0,wikidata_imdb:0,wikidata_tmdb:0,wikidata_retry:0,brave_fa:0,brave_no_candidate:0,brave_blocked:0,delay_ms:5000})}::jsonb,updated_at=now() WHERE id=${runId}`;await audit('brave_test_started',{total,delay_ms:5000,mode:'single_query'});for(let i=0;i<rows.length;i++){if(await cancelled())return stop(c);const row=rows[i],res=await batch([row.imdb_id],'fa_test');if(res?.cancelled)return stop(c);const st=res?.stats||{},x=res?.results?.[0];addStats(c,st);c.processed++;if(x?.ok&&x?.updated)c.updated++;if(!x?.ok){c.errors++;await audit('title_failed',{block:'Prueba Brave',imdb_id:row.imdb_id,error:x?.error||'unknown'})}await audit('brave_test_attempt',{index:i+1,imdb_id:row.imdb_id,title:row.title,method:x?.method||null,updated:Boolean(x?.updated),brave:x?.brave||null});await progress(c,total,'Prueba Brave · 30 · 5 s',i+1,total,row.title,{scope:'brave_test_30',delay_ms:5000});if(x?.brave?.blocked){const summary={stage:'brave_test_blocked',scope:'brave_test_30',current_block:'Prueba detenida al primer bloqueo Brave',progress_pct:Math.round(c.processed*1000/Math.max(total,1))/10,total_operations:total,operations:c.processed,...snapshot(c),blocked_at:i+1,blocked_imdb:row.imdb_id,elapsed_seconds:Math.round((Date.now()-started)/1000)};await finish('success',summary,c);await audit('brave_test_stopped_on_block',summary);console.log(JSON.stringify(summary,null,2));return}if(x?.brave&&i<rows.length-1)await sleep(5000)}const summary={stage:'brave_test_done',scope:'brave_test_30',current_block:'Prueba Brave completada',progress_pct:100,total_operations:total,operations:c.processed,...snapshot(c),elapsed_seconds:Math.round((Date.now()-started)/1000)};await finish('success',summary,c);await audit('brave_test_completed',summary);console.log(JSON.stringify(summary,null,2))}

async function main(){if(runMode==='brave_test')return braveTest();const tmdb=await sql`SELECT m.imdb_id,COALESCE(m.title_es,m.title,m.original_title) title FROM movies m LEFT JOIN catalog_exclusions ex USING(imdb_id) WHERE ex.imdb_id IS NULL AND m.imdb_id~'^tt[0-9]+$' AND m.tmdb_id IS NULL ORDER BY m.synced_at NULLS FIRST`;const fa=await sql`SELECT m.imdb_id,COALESCE(m.title_es,m.title,m.original_title) title FROM movies m LEFT JOIN catalog_exclusions ex USING(imdb_id) WHERE ex.imdb_id IS NULL AND m.imdb_id~'^tt[0-9]+$' AND m.fa_id IS NULL ORDER BY m.synced_at NULLS FIRST`;const total=tmdb.length+fa.length,c=counters();await sql`UPDATE pipeline_runs SET source='github',summary=COALESCE(summary,'{}'::jsonb)||${JSON.stringify({stage:'running',fa_strategy:'wikidata_then_python',total_operations:total,total_unique:new Set([...tmdb,...fa].map(x=>x.imdb_id)).size,progress_pct:0,current_block:'TMDb',found_total:0,not_found:0,wikidata_imdb:0,wikidata_tmdb:0,wikidata_retry:0,python_fa:0,python_ambiguous:0,python_not_found:0,python_errors:0,python_blocked:0,python_calls:0,python_delay_ms:2500,brave_fa:0,brave_no_candidate:0,brave_blocked:0})}::jsonb,updated_at=now() WHERE id=${runId}`;await audit('refresh_started',{tmdb_pending:tmdb.length,fa_pending:fa.length,total_operations:total,fa_strategy:'wikidata_then_python',block_size:8,python_delay_ms:2500});const first=await phase('TMDb','tmdb',tmdb,c,total);if(!first.ok||await cancelled())return stop(c);const second=await phase('FilmAffinity · Wikidata + Python','fa',fa,c,total);if(second.pythonBlocked){const s={stage:'paused_python_blocked',current_block:'FilmAffinity · Python detenido por bloqueo',...snapshot(c),elapsed_seconds:Math.round((Date.now()-started)/1000)};await finish('failed',s,c);await audit('refresh_paused_python_blocked',s);return}if(!second.ok||await cancelled())return stop(c);const[after]=await sql`SELECT count(*)::int remaining FROM movies m LEFT JOIN catalog_exclusions ex USING(imdb_id) WHERE ex.imdb_id IS NULL AND m.imdb_id~'^tt[0-9]+$' AND (m.tmdb_id IS NULL OR m.fa_id IS NULL)`;const summary={stage:'done',progress_pct:100,total_operations:total,operations:c.processed,updated:c.updated,...snapshot(c),remaining:Number(after?.remaining||0),elapsed_seconds:Math.round((Date.now()-started)/1000)};await finish('success',summary,c);await audit('refresh_completed',summary);console.log(JSON.stringify(summary,null,2))}
async function stop(c){const s={stage:'cancelled',cancelled:true,...snapshot(c),elapsed_seconds:Math.round((Date.now()-started)/1000)};await finish('failed',s,c);await audit('refresh_cancelled',{processed:c.processed,updated:c.updated,errors:c.errors,...s})}
main().catch(async e=>{const msg=e?.message||String(e);const c=counters();c.errors=1;try{await finish('failed',{stage:'failed',error:{message:msg},elapsed_seconds:Math.round((Date.now()-started)/1000)},c);await audit('refresh_failed',{error:msg})}catch{}console.error(e);process.exit(1)});
