import {validateIdentityEvidence} from '../lib/identity-title-normalization-core.mjs';
import {dataComplete} from '../lib/lifecycle-data-stage.mjs';
async function jsonFetch(url,options={},timeout=20000){const c=new AbortController(),t=setTimeout(()=>c.abort(),timeout);try{const r=await fetch(url,{...options,signal:c.signal});const text=await r.text();let d=null;try{d=JSON.parse(text)}catch{}if(!r.ok){const e=new Error(d?.status_message||d?.error||`HTTP ${r.status} en ${new URL(url).hostname}`);e.status=r.status;e.body=text;throw e}if(d===null)throw new Error(`JSON inválido desde ${new URL(url).hostname}`);return d}finally{clearTimeout(t)}}
const firstYear=v=>{const m=String(v||'').match(/(19|20)\d{2}/);return m?Number(m[0]):null};
async function ensureRow(sql,imdbId){await sql`INSERT INTO identity_validation(imdb_id,validation_status,validation_details,created_at,updated_at) VALUES(${imdbId},'pending_data','{}'::jsonb,now(),now()) ON CONFLICT(imdb_id) DO NOTHING`}
async function movie(sql,imdbId){const[r]=await sql`SELECT imdb_id,type,title,title_es,original_title,year,tmdb_id FROM movies WHERE imdb_id=${imdbId}`;if(!r)throw Object.assign(new Error('Título no encontrado'),{permanent:true});if(!r.tmdb_id)throw Object.assign(new Error('Necesita IMDb + TMDb antes de validar identidad'),{permanent:true});return r}
const mediaFor=t=>t==='Serie'||t==='Miniserie'?'tv':'movie';
export async function imdbEvidence(sql,imdbId){await ensureRow(sql,imdbId);const[v]=await sql`SELECT imdb_title,imdb_original_title,imdb_year FROM identity_validation WHERE imdb_id=${imdbId}`;if(v?.imdb_original_title&&v?.imdb_year)return{found:true,changed:false,result:{imdb_title:v.imdb_title,imdb_original_title:v.imdb_original_title,imdb_year:Number(v.imdb_year),method:'cache'}};let evidence=null;const[c]=await sql`SELECT year,source_snapshot FROM catalog_candidates WHERE imdb_id=${imdbId}`;if(c?.source_snapshot?.originalTitle&&c?.year)evidence={imdb_title:c.source_snapshot.title||null,imdb_original_title:c.source_snapshot.originalTitle,imdb_year:Number(c.year),method:'imdb_dataset_cache'};if(!evidence){const[m]=await sql`SELECT title,original_title,year FROM movies WHERE imdb_id=${imdbId}`;if(m?.original_title&&m?.year)evidence={imdb_title:m.title||null,imdb_original_title:m.original_title,imdb_year:Number(m.year),method:'catalog'}}if(!evidence){const key=process.env.OMDB_API_KEY||process.env.OMDB_APIKEY||process.env.OMDB_KEY;if(!key)return{found:false,changed:false,result:{status:'not_found',method:'catalog_incomplete'}};const d=await jsonFetch(`https://www.omdbapi.com/?apikey=${encodeURIComponent(key)}&i=${encodeURIComponent(imdbId)}&plot=short&r=json`,{headers:{'User-Agent':'PikoFilm/3.0'}});if(d?.Response==='False'||!d?.Title||d.Title==='N/A'||!firstYear(d?.Year))return{found:false,changed:false,result:{status:'not_found',method:'omdb'}};evidence={imdb_title:d.Title,imdb_original_title:d.Title,imdb_year:firstYear(d.Year),method:'omdb'}}await sql`UPDATE identity_validation SET imdb_title=${evidence.imdb_title||null},imdb_original_title=${evidence.imdb_original_title||null},imdb_year=${evidence.imdb_year||null},imdb_extracted_at=now(),validation_status='pending_data',validation_score=NULL,suspected_source=NULL,validated_at=NULL,updated_at=now() WHERE imdb_id=${imdbId}`;return{found:true,changed:true,result:evidence}}
export async function tmdbEvidence(sql,imdbId){const m=await movie(sql,imdbId);await ensureRow(sql,imdbId);const token=process.env.TMDB_API_TOKEN;if(!token)throw Object.assign(new Error('TMDB_API_TOKEN no configurado'),{permanent:true});const media=mediaFor(m.type),h={Authorization:`Bearer ${token}`,Accept:'application/json'};const[d,find,external]=await Promise.all([jsonFetch(`https://api.themoviedb.org/3/${media}/${m.tmdb_id}?language=es-ES`,{headers:h}),jsonFetch(`https://api.themoviedb.org/3/find/${encodeURIComponent(imdbId)}?external_source=imdb_id`,{headers:h}),jsonFetch(`https://api.themoviedb.org/3/${media}/${m.tmdb_id}/external_ids`,{headers:h})]);const date=d.release_date||d.first_air_date||'',list=media==='tv'?find.tv_results:find.movie_results,first=Array.isArray(list)&&list.length?list[0]:null,link={expected_imdb_id:String(imdbId),expected_tmdb_id:String(m.tmdb_id),expected_media_type:media,forward_found:Boolean(first),forward_tmdb_id:first?.id!=null?String(first.id):null,forward_media_type:first?media:null,forward_match:Boolean(first&&String(first.id)===String(m.tmdb_id)),reverse_imdb_id:external?.imdb_id||null,reverse_match:Boolean(external?.imdb_id&&String(external.imdb_id)===String(imdbId)),type_match:true};const evidence={tmdb_id:String(m.tmdb_id),tmdb_title_es:d.title||d.name||null,tmdb_original_title:d.original_title||d.original_name||null,tmdb_year:Number(date.slice(0,4))||null,method:'tmdb',link_evidence:link};const[v]=await sql`SELECT tmdb_id,tmdb_original_title,tmdb_year,validation_details FROM identity_validation WHERE imdb_id=${imdbId}`;const changed=String(v?.tmdb_id||'')!==String(m.tmdb_id)||String(v?.tmdb_original_title||'')!==String(evidence.tmdb_original_title||'')||Number(v?.tmdb_year||0)!==Number(evidence.tmdb_year||0)||JSON.stringify(v?.validation_details?.link_evidence||{})!==JSON.stringify(link);const manual=v?.validation_details?.manual;await sql`UPDATE identity_validation SET tmdb_id=${evidence.tmdb_id},tmdb_title_es=${evidence.tmdb_title_es},tmdb_original_title=${evidence.tmdb_original_title},tmdb_year=${evidence.tmdb_year},tmdb_extracted_at=now(),validation_details=${JSON.stringify({...((v?.validation_details)||{}),link_evidence:link,...(manual?{manual}:{})})}::jsonb,validation_status='pending_data',validation_score=NULL,suspected_source=NULL,validated_at=NULL,updated_at=now() WHERE imdb_id=${imdbId}`;return{found:Boolean(evidence.tmdb_original_title&&evidence.tmdb_year),changed,result:evidence}}
export async function faEvidence(){return{found:true,changed:false,reason:'FilmAffinity retirada de Validación; paso legacy sin llamada externa',result:{legacy:true,disabled:true}}}
async function nextStateAfterValid(sql,imdbId){const[r]=await sql`SELECT m.title_es,m.original_title,m.year,m.type,m.imdb_rating,m.imdb_votes,m.fa_rating,m.fa_votes,m.tmdb_rating,m.tmdb_votes,m.runtime,m.country,m.poster_path,mm.overview,COALESCE(m.source_status #>> '{data_quality_external_poster,url}','') external_poster_url,EXISTS(SELECT 1 FROM movie_genres g WHERE g.imdb_id=m.imdb_id) has_genres FROM movies m LEFT JOIN movie_metadata mm USING(imdb_id) WHERE m.imdb_id=${imdbId}`;return dataComplete(r)?{state:'PIKOSCORE_PENDING',reason:'Datos completos; PikoScore pendiente o caducado'}:{state:'DATA_INCOMPLETE',reason:'Ficha de datos incompleta'}}
export async function scoreIdentity(sql,imdbId){await ensureRow(sql,imdbId);const[v]=await sql`SELECT * FROM identity_validation WHERE imdb_id=${imdbId}`;const tmdbFresh=v?.tmdb_extracted_at&&Date.now()-new Date(v.tmdb_extracted_at).getTime()<=120000;if(!tmdbFresh)throw new Error('Evidencia TMDb no refrescada en esta ejecución; se cancela el scoring para no usar caché antigua');const link=v?.validation_details?.link_evidence||{};if(!Object.keys(link).length)throw new Error('Falta evidencia de vínculo IMDb↔TMDb de esta ejecución');const evidence={imdb_title:v?.imdb_title,imdb_original_title:v?.imdb_original_title,imdb_year:v?.imdb_year,tmdb_id:v?.tmdb_id,tmdb_title_es:v?.tmdb_title_es,tmdb_original_title:v?.tmdb_original_title,tmdb_year:v?.tmdb_year,link_evidence:link},validation=validateIdentityEvidence(evidence),manual=v?.validation_details?.manual,manualValid=manual&&manual.status&&String(manual.imdb_id||'')===String(imdbId)&&String(manual.tmdb_id||'')===String(evidence.tmdb_id||''),details={...validation.details,...(manualValid?{manual}:{})},effective=manualValid?manual.status:validation.status;await sql`UPDATE identity_validation SET validation_status=${effective},validation_score=${validation.score},validation_details=${JSON.stringify(details)}::jsonb,suspected_source=${validation.suspected},validated_at=now(),updated_at=now() WHERE imdb_id=${imdbId}`;let next;if(effective==='valid')next=await nextStateAfterValid(sql,imdbId);else if(['doubtful','invalid'].includes(effective))next={state:'IDENTITY_REVIEW_REQUIRED',reason:`Validación de identidad: ${effective}`};else next={state:'IDENTITY_VALIDATION',reason:'Identidad pendiente de validación'};await sql`UPDATE catalog_lifecycle SET previous_state=CASE WHEN lifecycle_state<>${next.state} THEN lifecycle_state ELSE previous_state END,lifecycle_state=${next.state},blocking_reason=${next.reason},state_changed_at=CASE WHEN lifecycle_state<>${next.state} THEN now() ELSE state_changed_at END,computed_at=now() WHERE imdb_id=${imdbId}`;return{found:effective!=='insufficient',changed:next.state!=='IDENTITY_VALIDATION',result:{status:effective,automatic_status:validation.status,score:validation.score,suspected_source:validation.suspected,lifecycle:next.state,reason:next.reason,details}}}
