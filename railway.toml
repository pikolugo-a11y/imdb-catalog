[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.technical"

[deploy]
startCommand = "node -e \"console.log('technical snapshot disabled pending explicit approval')\""
restartPolicyType = "NEVER"
restartPolicyMaxRetries = 0
