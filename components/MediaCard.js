import Link from 'next/link';
export function Poster({path,title}){const src=path?`https://image.tmdb.org/t/p/w342${path}`:null;return <div className="poster">{src?<img src={src} alt={`Carátula de ${title}`} loading="lazy"/>:<div className="poster-empty">{(title||'?').slice(0,1)}</div>}</div>}
export function MediaCard({item,href}){return <Link href={href} className="media-card"><Poster path={item.poster_path} title={item.display_title||item.title}/><div className="media-info"><div className="media-title">{item.display_title||item.title}</div><div className="media-meta"><span>{item.year||'—'}</span>{item.final_rating!=null&&<b>★ {Number(item.final_rating).toFixed(1)}</b>}</div><div className="media-bottom">{item.effective_status==='in_plex'?<span className="status ok">En Plex</span>:item.effective_status==='acquiring'?<span className="status warn">En proceso</span>:<span className="status bad">Falta</span>}</div></div></Link>}
