import Link from 'next/link';
import BatchAutoRefresh from './BatchAutoRefresh';
import {startData001BatchAction,pauseData001BatchAction,resumeData001BatchAction,cancelData001BatchAction} from '@/app/calidad/datos/batch-actions';
const nf=n=>Number(n||0).toLocaleString('es-ES');
const pct=(done,total)=>total?Math.min(100,Math.round((Number(done||0)*1000)/Number(total))/10):0;
export default function Data001BatchPanel({state}){const active=state?.active||null,latest=state?.latest||null,enginePaused=state?.engine?.desired_state==='paused',run=active||latest,total=Number(run?.items_total||0),processed=Number(run?.items_processed||0),succeeded=Number(run?.items_succeeded||0),failed=Number(run?.items_failed||0),pending=Number(run?.items_pending||0),paused=active?.desired_state==='paused'||enginePaused,cancelling=active?.desired_state==='cancel_requested';return <section className="batch-panel" aria-label="Batch datos estructurales"><BatchAutoRefresh active={Boolean(active)} paused={paused}/><div className="batch-panel-head"><div><span className="cap">Batch · DATA-001</span><h2>Completar datos masivamente</h2><p>Ejecuta el mismo proceso canónico del botón individual: completa sólo los campos ausentes con TMDb → OMDb → MDBList, sin pisar datos ya informados.</p></div><span className={`batch-pill ${active?paused?'paused':'running':'idle'}`}>{active?(cancelling?'Cancelando':paused?'Pausado':'En curso'):'Disponible'}</span></div>{!active&&<div className="batch-idle"><div><strong>{nf(state?.eligibleCount)} pendientes</strong><small>Solo títulos en DATA_INCOMPLETE con identidad validada. Concurrencia 2 y gobierno común de APIs.</small></div><form action={startData001BatchAction}><select name="limit" defaultValue="1" aria-label="Tamaño Batch datos"><option value="1">1 título</option><option value="5">5 títulos</option><option value="25">25 títulos</option><option value="">Todos</option></select><button className="button" disabled={!state?.eligibleCount}>Iniciar Batch</button></form></div>}{active&&<><div className="batch-progress-line"><div className="batch-progress"><span style={{width:`${pct(processed,total)}%`}}/></div><b>{nf(processed)} / {nf(total)} · {pct(processed,total)} %</b></div><div className="batch-kpis"><span>✓ Correctos <b>{nf(succeeded)}</b></span><span>⚠ Errores <b>{nf(failed)}</b></span><span>Pendientes <b>{nf(pending)}</b></span><span>Concurrencia <b>{nf(active.requested_concurrency)}</b></span></div>{paused&&<p className="batch-warning">Pausado{active?.pause_reason?` · ${String(active.pause_reason).replace('source_quota:','cuota agotada: ').replace('source_throttled:','limitación de fuente: ')}`:''}. Los items ya terminados se conservan.</p>}<div className="batch-actions">{!cancelling&&(paused?<form action={resumeData001BatchAction}><input type="hidden" name="runId" value={active.run_id}/><button className="button">Continuar</button></form>:<form action={pauseData001BatchAction}><input type="hidden" name="runId" value={active.run_id}/><button className="button ghost">Pausar</button></form>)}<form action={cancelData001BatchAction}><input type="hidden" name="runId" value={active.run_id}/><button className="button ghost">Cancelar</button></form><Link className="button ghost" href={`/admin/runs/${active.run_id}`}>Ver en Centro de Operaciones</Link></div></>}{!active&&latest&&<div className="batch-last"><span>Última ejecución: <b>{latest.technical_status}</b> · {nf(processed)}/{nf(total)}</span><Link href={`/admin/runs/${latest.run_id}`}>Ver detalle →</Link></div>}<div className="batch-last"><span>TMDb hoy: <b>{nf(state?.tmdb?.batch_calls)}</b> Batch · OMDb: <b>{nf(state?.omdb?.batch_calls)}</b> · MDBList: <b>{nf(state?.mdblist?.batch_calls)}</b></span><Link href="/admin">Límites API →</Link></div></section>}
