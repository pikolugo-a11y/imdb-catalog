import {updateBatchApiSourceAction,closeBatchApiBreakerAction} from '@/app/admin/batch-api-actions';
const nf=n=>Number(n||0).toLocaleString('es-ES');
export default function OperationsApiSources({sources=[]}){return <section className="ops-panel"><div className="ops-panel-head"><div><span className="ops-label">Batch API</span><h2>Fuentes y límites</h2></div></div><div className="ops-api-sources">{sources.map(s=><article key={s.source}><div><strong>{s.source.toUpperCase()}</strong><span>{nf(s.batch_calls)} Batch · {nf(s.manual_calls)} manual · {nf(s.active_leases)} activas</span><small>{s.breaker_state==='open'?`Breaker abierto${s.blocked_until?` hasta ${new Date(s.blocked_until).toLocaleTimeString('es-ES')}`:''}`:'Breaker cerrado'} · {nf(s.rate_limited)} rate-limit hoy</small></div><form action={updateBatchApiSourceAction}><input type="hidden" name="source" value={s.source}/><label>Límite/día<input name="dailyLimit" defaultValue={s.daily_limit??''} placeholder={s.source==='tmdb'?'sin límite':'0'}/></label><label>% Batch<input name="batchSharePercent" type="number" min="1" max="99" defaultValue={s.batch_share_percent}/></label><label>Concurrencia<input name="maxConcurrency" type="number" min="1" max="8" defaultValue={s.max_concurrency}/></label><button>Guardar</button></form>{s.breaker_state==='open'&&<form action={closeBatchApiBreakerAction}><input type="hidden" name="source" value={s.source}/><button>Cerrar breaker</button></form>}</article>)}</div></section>}
