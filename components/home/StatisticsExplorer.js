'use client';
import Link from 'next/link';
import {useEffect,useMemo,useState} from 'react';
import {nf,one,pct,score,pp,decadeLabel} from '@/lib/home-format';
import {eligibleRows,mostCovered,leastCovered,bestScore} from '@/lib/home-statistics';
const tabs=[['dna','ADN'],['coverage','Cobertura'],['score','PikoScore'],['countries','Países'],['sagas','Sagas'],['profile','Perfil'],['balance','Balance']];
const genreHref=(label,status='')=>`/catalogo?genres=${encodeURIComponent(label)}${status?`&status=${status}`:''}`;
const decadeHref=b=>`/catalogo?yearFrom=${b}&yearTo=${Number(b)+9}`;
function Bars({rows,value='total',suffix='',integer=false,diverging=false,hrefFor}){const safe=(rows||[]).filter(Boolean),max=Math.max(...safe.map(r=>Math.abs(Number(r?.[value]||0))),1);return <div className={`home-bars ${diverging?'diverging':''}`}>{safe.map((r,i)=>{const raw=Number(r?.[value]||0),label=r.label||r.band||(r.bucket!=null?decadeLabel(r.bucket):'Sin etiqueta'),inside=<><span>{label}</span><div><i className={raw<0?'negative':''} style={{width:`${Math.max(3,Math.round(Math.abs(raw)/max*100))}%`}}/></div><b>{integer?nf(raw):one(raw)}{suffix}</b></>;return hrefFor?<Link className="home-bar navigable" href={hrefFor(r)} key={`${label}-${i}`}>{inside}</Link>:<div className="home-bar" key={`${label}-${i}`}>{inside}</div>})}</div>}
function Insight({label,value,help,tone='',href}){const body=<><span>{label}</span><b>{value}</b>{help&&<small>{help}</small>}</>;return href?<Link className={tone} href={href}>{body}</Link>:<article className={tone}>{body}</article>}
export default function StatisticsExplorer({data}){const[tab,setTab]=useState('dna');useEffect(()=>{const saved=window.localStorage.getItem('pikofilm-home-stat');if(tabs.some(([id])=>id===saved))setTab(saved)},[]);const choose=id=>{setTab(id);window.localStorage.setItem('pikofilm-home-stat',id)};const genreEligible=useMemo(()=>eligibleRows(data.genres||[],'genre'),[data.genres]);const countryEligible=useMemo(()=>eligibleRows(data.countries||[],'country'),[data.countries]);const decadeEligible=useMemo(()=>eligibleRows(data.decades||[],'decade'),[data.decades]);const gap=useMemo(()=>[...genreEligible].sort((a,b)=>a.coverage-b.coverage).slice(0,7),[genreEligible]);const country=(data.countries||[]).slice(0,8);const balance=useMemo(()=>[...(data.balance?.genres||[])].filter(r=>genreEligible.some(g=>g.label===r.label)).sort((a,b)=>Math.abs(b.delta)-Math.abs(a.delta)).slice(0,7),[data.balance,genreEligible]);const topGenre=(data.genres||[])[0]||null,bestCoverage=mostCovered(data.genres||[],'genre'),topScore=bestScore(data.genres||[],'genre'),bestDecade=mostCovered(data.decades||[],'decade'),bestCountry=mostCovered(data.countries||[],'country'),saga=data.sagas||{},profile=data.profile||{};const durationRows=[{label:'< 90 min',total:profile.runtime_lt90},{label:'90–119 min',total:profile.runtime_90_119},{label:'120–149 min',total:profile.runtime_120_149},{label:'150–179 min',total:profile.runtime_150_179},{label:'180+ min',total:profile.runtime_180_plus}];const runtimeValid=Number(profile.movie_runtime_known||0)>0;
return <section className="dashboard-panel stats-explorer"><div className="section-head home-section-head"><div><div className="eyebrow">Mi filmoteca</div><h2>Explorador estadístico</h2><p>Los datos son explorables: cuando existe un filtro canónico, una fila te lleva al detalle.</p></div></div><div className="stats-tabs" role="tablist" aria-label="Estadísticas de la filmoteca">{tabs.map(([id,label])=><button key={id} role="tab" aria-selected={tab===id} className={tab===id?'active':''} onClick={()=>choose(id)}>{label}</button>)}</div><div className={`stats-stage signature-${tab}`}>
{tab==='dna'&&<div className="stats-grid editorial"><div><h3>ADN de la colección</h3><p className="stats-lead">Huella de géneros: un título puede pertenecer a varios.</p><div className="dna-signature"><Bars rows={(data.genres||[]).slice(0,8)} integer hrefFor={r=>genreHref(r.label)}/></div></div><div className="stat-insights"><Insight label="Género dominante" value={topGenre?.label||'—'} help={topGenre?`${nf(topGenre.total)} títulos asociados`:null} href={topGenre?genreHref(topGenre.label):null}/><Insight label="Mejor cobertura representativa" value={bestCoverage?.label||'—'} help={bestCoverage?`${pct(bestCoverage.coverage)} · ${nf(bestCoverage.owned)}/${nf(bestCoverage.total)}`:null} href={bestCoverage?genreHref(bestCoverage.label):null}/><Insight label="Mejor PikoScore representativo" value={topScore?.label||'—'} help={topScore?`${score(topScore.avg_score)} de media`:null} href={topScore?genreHref(topScore.label):null}/></div></div>}
{tab==='coverage'&&<div className="stats-grid editorial"><div><h3>Mapa de cobertura</h3><p className="stats-lead">Huecos representativos por género y lectura temporal por décadas.</p><Bars rows={gap} value="coverage" suffix=" %" hrefFor={r=>genreHref(r.label,'missing')}/></div><div className="stat-insights"><Insight label="Mayor hueco representativo" value={leastCovered(data.genres||[],'genre')?.label||'—'} help={gap[0]?`${pct(gap[0].coverage)} · ${nf(gap[0].owned)}/${nf(gap[0].total)}`:null} href={gap[0]?genreHref(gap[0].label,'missing'):null}/><Insight label="Década más cubierta" value={bestDecade?decadeLabel(bestDecade.bucket):'—'} help={bestDecade?`${pct(bestDecade.coverage)} · ${nf(bestDecade.owned)}/${nf(bestDecade.total)}`:'Sin muestra suficiente'} href={bestDecade?decadeHref(bestDecade.bucket):null}/></div><div className="coverage-mini-grid heatmap-row">{decadeEligible.slice(-8).map(r=><Link href={decadeHref(r.bucket)} key={r.bucket} style={{'--heat':`${Math.max(8,r.coverage)}%`}}><span>{decadeLabel(r.bucket)}</span><b>{pct(r.coverage)}</b><small>{nf(r.owned)}/{nf(r.total)}</small></Link>)}</div></div>}
{tab==='score'&&<div className="stats-grid editorial"><div><h3>PikoScore Observatory</h3><p className="stats-lead">Distribución por bandas canónicas y cobertura real dentro de cada banda.</p><div className="score-distribution"><Bars rows={data.scoreBands||[]} value="coverage" suffix=" %"/></div></div><div className="stat-insights"><Insight label="PikoScore medio" value={score(data.kpi.avg_score)} help="catálogo objetivo"/><Insight label="PikoScore medio en Plex" value={score(data.kpi.avg_score_plex)} help="biblioteca presente"/><Insight label="Títulos con PikoScore" value={pct(profile.score_coverage)} help={`${nf(data.kpi.score_known)} de ${nf(data.kpi.catalog_total)}`}/></div><div className="score-band-cards">{(data.scoreBands||[]).map(r=><article key={r.band||r.label}><span>{r.label||r.band||'Sin banda'}</span><b>{pct(r.coverage)}</b><small>{nf(r.owned)}/{nf(r.total)} en Plex</small></article>)}</div></div>}
{tab==='countries'&&<div className="stats-grid editorial"><div><h3>Cinematografías</h3><p className="stats-lead">Constelación de presencia. Las coproducciones pueden pertenecer a más de un país.</p><div className="country-constellation">{country.slice(0,6).map((r,i)=><span key={r.label} style={{'--rank':i}} title={`${r.label}: ${nf(r.total)}`}>{r.label}<b>{nf(r.total)}</b></span>)}</div><Bars rows={country} integer/></div><div className="stat-insights"><Insight label="Más representada" value={country[0]?.label||'—'} help={country[0]?`${nf(country[0].total)} títulos asociados`:null}/><Insight label="Mejor cubierta representativa" value={bestCountry?.label||'—'} help={bestCountry?`${pct(bestCountry.coverage)} · ${nf(bestCountry.owned)}/${nf(bestCountry.total)}`:null}/><Insight label="Países visibles" value={nf(countryEligible.length)} help="con muestra suficiente entre los principales"/></div></div>}
{tab==='sagas'&&<div className="stats-grid saga-rich"><div><h3>Sagas & universos</h3><p className="stats-lead">Radiografía canónica de completitud.</p><div className="saga-dual"><div className="saga-orbit"><strong>{pct(saga.complete_pct)}</strong><span>sagas completas</span></div><div className="saga-orbit secondary"><strong>{pct(saga.coverage)}</strong><span>cobertura de universos</span></div></div></div><div className="stat-insights"><Insight label="Completas" value={nf(saga.complete)} help={`de ${nf(saga.all)} colecciones activas`}/><Insight label="A una película" value={nf(saga.one)} help="victorias rápidas" href="/sagas?state=one"/><Insight label="Incompletas" value={nf(saga.incomplete)} help="con al menos una pendiente"/><Insight label="Sin empezar" value={nf(saga.not_started)} help="0 títulos relevantes en Plex"/></div><div className="saga-summary-line"><span><b>{nf(saga.owned_movies)}</b> de {nf(saga.movies)} títulos de saga en Plex</span><span><b>{nf(saga.missing_movies)}</b> pendientes accionables</span></div></div>}
{tab==='profile'&&<div className="stats-grid editorial"><div><h3>Perfil de tu filmoteca</h3><div className="profile-line"><span>{profile.oldest_year||'—'}</span><i/><b>{Math.round(Number(profile.median_year||0))||'—'}</b><i/><span>{profile.newest_year||'—'}</span></div><p className="stats-lead">Año mediano: {Math.round(Number(profile.median_year||0))||'—'}. Duración sólo cuando existe una muestra válida.</p>{runtimeValid?<Bars rows={durationRows} integer/>:<div className="metric-unavailable"><b>Duración no disponible</b><span>La fuente canónica todavía no aporta runtime válido; no lo representamos como 0.</span></div>}</div><div className="stat-insights"><Insight label="Duración mediana" value={runtimeValid&&profile.median_runtime?`${Math.round(Number(profile.median_runtime))} min`:'—'} help={runtimeValid?`${pct(profile.runtime_coverage)} con runtime conocido`:'Sin muestra válida'}/><Insight label="Más de 3 horas" value={runtimeValid?nf(profile.runtime_180_plus):'—'} help={runtimeValid?'películas':'Sin muestra válida'}/><Insight label="Horas de cine en Plex" value={runtimeValid?nf(profile.plex_movie_hours):'—'} help={runtimeValid?'sólo películas con runtime válido':'Sin muestra válida'}/></div></div>}
{tab==='balance'&&<div className="stats-grid editorial"><div><h3>Balance de colección</h3><p className="stats-lead">← menos presente · 0 · más presente →. Participación relativa normalizada.</p><div className="balance-mirror"><Bars rows={balance} value="delta" suffix=" pp" diverging hrefFor={r=>genreHref(r.label)}/></div></div><div className="stat-insights"><Insight label="Mayor desviación" value={balance[0]?.label||'—'} help={balance[0]?`${pp(balance[0].delta)} · ${Number(balance[0].delta)>=0?'más presente en Plex':'menos presente en Plex'}`:null} href={balance[0]?genreHref(balance[0].label):null}/><Insight label="Comparación" value="Catálogo ↔ Plex" help="distribuciones normalizadas, no faltantes absolutos"/></div></div>}
</div></section>}
