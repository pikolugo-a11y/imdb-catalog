'use client';
import {useActionState} from 'react';
export default function ActionButton({action,label,pendingLabel='Procesando…',className='',fields={},confirmMessage=''}){const[state,formAction,pending]=useActionState(action,null);const onSubmit=event=>{if(confirmMessage&&!pending&&!window.confirm(confirmMessage))event.preventDefault()};return <div className="action-stack"><form action={formAction} onSubmit={onSubmit}>{Object.entries(fields).map(([k,v])=><input key={k} type="hidden" name={k} value={v??''}/>)}<button className={className} disabled={pending}>{pending?pendingLabel:label}</button></form>{state?.message&&<small className={state.ok?'action-ok':'action-error'}>{state.message}</small>}</div>}
